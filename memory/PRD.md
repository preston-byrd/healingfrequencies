# PRD — Healing Frequencies

## Original problem statement
"Can you create healing frequencies"

## User choices (from initial ask_human)
- Both: preset Solfeggio + custom frequency generator
- Features: timer/session length, background visuals, ambient nature sounds layer mixed with tones, save favorite sessions, breathwork guide synced to tones
- Pure Web Audio API (no audio files)
- Design vibe: blend of calm/minimal, cosmic/mystical, nature/earthy
- Auth required (JWT email + password)

## Architecture
- **Backend:** FastAPI + Motor (async MongoDB). JWT auth in httpOnly cookies (SameSite=None, Secure) with Authorization Bearer fallback.
- **Frontend:** React 19 (CRA + craco). Tailwind CSS, lucide-react icons. Canvas-based visualizer.
- **Audio:** Web Audio API singleton engine. Oscillator for tone (sine/triangle/square/sawtooth) + optional binaural offset via channel-split. Three ambient layers (rain/ocean/forest) generated from pink-ish noise + biquad filters (low-pass for ocean with LFO, high-pass for rain, band-pass for forest).
- **DB collections:** `users` (id, email, name, password_hash, role, created_at), `sessions` (id, user_id, name, frequency, waveform, binaural, duration_minutes, ambient, breathwork, created_at).

## User personas
- Wellness seeker who opens the app for a quick guided sound-bath / meditation session.
- Practitioner / power user who composes custom mixes (frequency + binaural + ambient layers) and saves them.

## Core requirements (static)
- Email/password auth (register, login, logout, /me)
- Solfeggio preset grid (174, 285, 396, 417, 432, 528, 639, 741, 852, 963 Hz)
- Custom frequency slider 20–1200 Hz with waveform selector
- Binaural offset 0–40 Hz
- Tone volume control
- Ambient mixer (rain / ocean / forest) with independent volume
- Session timer 1–60 min with on-screen countdown
- Animated visualizer (canvas; pulses with frequency + play state)
- Breathwork guide overlay (4-4-6 cycle, phase label)
- Saved sessions (CRUD scoped to logged-in user) with one-click load

## Implemented (Jan 2026)
- All core requirements above
- Aurora/grain ambient backdrop, glassmorphism panels, custom sliders (teal for tone, amber for ambient)
- JWT cookie + Bearer fallback (so cross-origin works seamlessly)
- Admin seeded automatically on startup (admin@example.com / admin123)
- **Daily check-in streak** (Jan 2026): `/api/streak` + `/api/streak/checkin`. Auto check-in after ≥60s of continuous playback. Streak/longest/total-min displayed in left sidebar.
- **Gentle daily reminder** via Web Notifications API. User opts in, picks a time, and a daily notification is scheduled in-browser ("A quiet moment awaits 🌿").
- **Golden Ratio (φ) Stack** (Jan 2026): dedicated preset (144 · 233 · 377 Hz Fibonacci chord) + toggle that stacks φ¹ and φ² harmonics on any base frequency.
- **PWA** (Jan 2026): manifest, service worker for offline shell, app icons, installable on iOS/Android/desktop.
- **Subscription & billing** (Jan 2026): Basic (free, 3-session cap) + Pro Monthly ($9.99/mo) + Pro Annual ($60/yr). 7-day free trial. Stripe Checkout via emergentintegrations. `/api/me/checkout`, `/api/payments/status/{id}`, `/api/webhook/stripe`. Pro unlocks Golden Stack, ambient layers, breathwork, custom freq generator, unlimited saves.
- **Account dashboard** (Jan 2026): change password, current plan + days left, upgrade flow, billing history, admin price editor.
- **Admin user management** (Jan 2026): `/api/admin/users` list + search by email; `/api/admin/users/{id}/grant-pro` (extend from existing pro_until if active); `/api/admin/users/{id}/revoke-pro`; **DELETE /api/admin/users/{id}** (cascade-deletes sessions + streaks + transactions; cannot delete self or other admins). UI in AccountDashboard with per-row days input, Grant/Extend + Revoke + Delete buttons.
- **Admin = lifetime Pro** (Jan 2026): admins automatically bypass paywall (sub.pro=true, plan=admin). Self-healing admin seeding always sets role=admin on restart.
- **More frequencies** (Jan 2026): 10 new "Brainwave & Specials" presets (Delta 2 Hz, Theta 6 Hz, Schumann 7.83 Hz, Alpha 10 Hz, Gamma 40 Hz, 111, 222, Tesla 369, Angel 444, 1111 Hz). Custom freq slider now 1–1200 Hz (was 20–1200).
- **More ambient layers** (Jan 2026): 5 new layers added (wind, crickets, singing bowls, brown noise, white noise) → 8 total. Each generated live via filtered noise + per-layer LFO modulation.
- **Ocean-at-0% bug fix** (Jan 2026): refactored ambient audio graph so the LFO modulates an inner `modGain` node, never the user-controlled `userGain`. `setAmbient(kind, 0)` now does `cancelScheduledValues` + `setValueAtTime(0)` for an instant hard mute.
- **Sleep Mode** (Jan 2026): one-tap preset = 4 Hz Theta–Delta + brown noise @ 45% + 30-min timer with 60-second linear fade-to-silence on tone AND every ambient gain. Auto-clear gated on `remaining === 0` (avoids race with deferred audio start). Pro-gated.
- **Brainwave & Specials Pro paywall** (Jan 2026): all 10 special-freq presets (Delta, Theta, Schumann, Alpha, Gamma, 111, 222, Tesla 369, Angel 444, 1111) are now Pro-gated via `selectFrequency(hz, {special:true})`. Locked-state UI: Lock icon next to title, PRO badge, dimmed grid + pointer-events disabled, centered "Included in Pro" CTA. Admin and Pro users get unrestricted access.
- **Audio reliability fix** (Jan 2026): `_ensureCtx()` async + awaits `ctx.resume()`; `start()` async with `_starting` guard; `stop()` uses local osc refs + immediate null of instance refs (race-window safe); window-level first-gesture unlock listener (`pointerdown/touchstart/keydown`) → audioEngine.unlock() for iOS Safari.
- Backend + frontend tested end-to-end (testing agent iterations 1–9: 100% pass)

## Implemented (Feb 2026)
- **Tap-to-toggle frequency** (Feb 2026): Tapping any frequency button (Solfeggio / Brainwave & Specials / φ Golden Stack) while that same selection is currently playing now STOPS playback. Tapping a DIFFERENT frequency while playing live-retunes (does not stop). Implemented in `Dashboard.jsx::selectFrequency` via `sameFreq` (`Math.abs(audioEngine.frequency - hz) < 0.05`) + `sameMode` (matching Golden-Stack state).
- **Code quality cleanup** (Feb 2026): Memoized `AuthContext` + `SubscriptionContext` provider values with `useMemo` / `useCallback` (prevents unnecessary consumer re-renders). Replaced silent `catch {}` blocks with `console.warn(...)` logging in `Dashboard.jsx`, `AccountDashboard.jsx`, and both contexts. Backend pytest admin credentials are now env-driven (`ADMIN_TEST_EMAIL` / `ADMIN_TEST_PASSWORD` with safe defaults). `audioEngine.js` left intentionally untouched (its noop catches handle iOS unlock + cleanup races).
- Testing: iteration 14 — 12/12 frontend scenarios pass, 44/47 pytest pass (2 pre-existing stale checkout-tx tests, 1 intentional skip).

## Implemented (Feb 2026 — cont.)
- **Edit user name** (Feb 2026): Users can now edit their display name from AccountDashboard. New endpoint `PUT /api/me/profile` with Pydantic `ProfileUpdateIn` (min_length=1, max_length=80, whitespace-trim). `AuthContext` exposes `setUserName(name)` so the in-memory user object updates immediately and propagates to the Dashboard sidebar greeting. Inline edit UI with Edit / Save / Cancel and auto-focus on the input. Testing iteration 15: 6/6 backend + 10/10 frontend scenarios pass.

- **Stripe checkout — surface real errors instead of silent fail** (Feb 2026): When a user replaces `sk_test_emergent` with their own `sk_live_...` or real `sk_test_...` key, the emergentintegrations library correctly routes to `api.stripe.com` (per library L107-109). If Stripe rejects the key (invalid / restricted account / unsupported currency), `/api/me/checkout` now catches the exception, logs the traceback, and returns HTTP 502 with the underlying message (e.g., `"Stripe checkout failed: Invalid API Key provided..."`). Backend also rejects empty `session.url` responses with 502. Frontend `upgrade()` in `AccountDashboard.jsx` guards against `undefined data.url` and scrolls to top on any error so the small `account-err` div is visible. Testing iteration 16: 6/6 backend + 5/5 frontend scenarios pass.

- **Stale pytest cleanup + Thank-You celebration + Persisted last-used config** (Feb 2026): Three features shipped together.
  - Removed 2 stale `test_checkout_{monthly,annual}_creates_transaction` tests from `test_subscription.py` (superseded by `test_billing_history.py`); also removed unused `import time`.
  - NEW component `ThankYouCelebration.jsx` — cosmic celebration overlay with 36 CSS-only confetti particles + 3 pulsing rings + a φ glyph. Fires from `AccountDashboard` payment-status polling when `payment_status === 'paid'`. CTA "Start your first Pro session" closes the modal and returns to the player. Plan label reads "Pro Monthly" / "Pro Annual" based on the original tx.plan (added to `/api/payments/status/{sid}` response).
  - NEW endpoints `GET /api/me/prefs` and `PUT /api/me/prefs` with Pydantic-validated `PrefsIn` (frequency 0.1-20000, duration 1-180, waveform sine/triangle/square/sawtooth, binaural 0-40, tone_volume 0-1, ambient as Dict[str, float], golden_stack/breathwork booleans). Stored in `users.prefs` subdoc with dotted-path `$set` merge so partial PUTs preserve other fields. Backend defense-in-depth: PUT silently strips Pro-only fields (`golden_stack`, `breathwork`, `binaural`, `ambient`) when the user isn't Pro, so a stale UI state can't clobber saved Pro values after a downgrade.
  - Dashboard.jsx restores prefs on mount (waits for `/me/subscription` to resolve before evaluating `isPro`, then runs exactly once via `restoreStartedRef`). Debounced auto-save (1.2s) fires on any knob change; payload omits Pro-only fields when `isPro=false`. Auto-save effect deps include `isPro` so it re-runs immediately if the user upgrades mid-session.
  - Testing iterations 17/18/19 — converged on full pass: backend 24/24 (15 prefs + 8 subscription + 1 payments-status), frontend 100% on the full upgrade flow (saved Pro knobs survive logout/login + restore correctly after trial upgrade). Two race conditions caught and fixed: (i) iter-17 stale `isPro` closure in restore effect, (ii) iter-18 auto-save clobber of Pro-only fields with React defaults for non-Pro users.

- **4-payment-method upgrade flow** (Feb 2026): Apple Pay / Google Pay / Card / Payment Link, with device-aware capability detection.
  - Architectural insight: a Stripe Checkout Session URL IS a sharable cross-device URL. So all 4 methods hit the SAME `/api/me/checkout` endpoint and use the SAME Stripe session. Apple Pay / Google Pay / Card redirect this device to Stripe Checkout (which auto-renders Apple Pay / Google Pay buttons inside its hosted page when the browser supports them). Payment Link opens a modal with a QR code (`qrcode.react`) + copy-to-clipboard + open-in-new-tab — same URL, different presentation.
  - NEW `usePaymentMethodSupport.js` hook detects wallets: Apple Pay via `window.ApplePaySession.canMakePayments()`, Google Pay via the `PaymentRequest` API with `google.com/pay` supportedMethods. Returns `{applePay, googlePay, ready}`; AP/GP buttons hidden when unsupported, "wallet-unavailable-hint" shown when both fail.
  - NEW `PaymentLinkModal.jsx` — QR (M error correction, 188px), URL input with auto-select-on-click, copy-with-clipboard-fallback, open-in-new-tab anchor (target=_blank), outside-click + X-button close.
  - Backend `CheckoutIn` gained `payment_method_preference: card|apple_pay|google_pay|link` (Pydantic regex pattern, defaults to 'card'). Forwarded to Stripe metadata for analytics; does NOT change the Stripe call.
  - AccountDashboard upgrade card refactored: 2-card plan selector (Monthly/Annual with aria-pressed) + 4-button payment-methods grid below.
  - Testing iteration 20 — 38/38 backend (12 new payment_methods tests + full regression) + 13/13 frontend e2e (plan switch, wallet hide+hint, link modal w/ QR/copy/open/close, annual title, card-redirect). Zero defects.

- **Stripe blank-checkout production bug — root caused and fixed** (Feb 2026): User reported Stripe Checkout loads a blank skeleton page in production with their `sk_live_...` key. RCA: the `emergentintegrations` library mutates the module-level `stripe.api_base` singleton when the key contains `sk_test_emergent` (lib L107-109) but NEVER resets it for non-emergent keys. Because Python module state persists for the entire uvicorn worker lifetime, ANY prior `sk_test_emergent` call (a startup probe, a previous deployment, or a brief `.env` placeholder) leaves `stripe.api_base` stuck pointing at Emergent's proxy. Subsequent `sk_live_` calls then create Checkout Sessions on Emergent's proxy account → returned session URLs reference IDs that don't exist in the user's real Stripe account → Stripe Checkout fetches and fails silently → blank page.
  - FIX: new helper `_stripe_client(webhook_url)` in `server.py` L455-475 that explicitly normalises `stripe.api_base` BEFORE every `StripeCheckout` instantiation — Emergent's proxy iff key contains `sk_test_emergent`, else `https://api.stripe.com`. All 3 call sites (`POST /me/checkout`, `GET /payments/status/{sid}`, `POST /webhook/stripe`) refactored to use it.
  - Added diagnostic `logger.info` line in `/me/checkout` printing the resolved `api_base` + `key_prefix` for operator-visibility in `/var/log/supervisor/backend.err.log`.
  - Testing iteration 21 — 9 new sticky-state guard tests in `test_stripe_routing.py` (4 unit monkeypatch tests proving deterministic api_base recovery + 4 live HTTP regression + 1 log-line assertion) + 38 regression. All 47/47 pass.

- **Soundscape playback — graceful fade + tap-to-toggle + pause/resume** (Feb 2026): Comprehensive overhaul of the Soundscape player flow.
  - `audioEngine.setAmbient(k, 0)` no longer instant-cuts; it now `cancelScheduledValues` + `setValueAtTime(current)` + `linearRampToValueAtTime(0, +0.8s)` — every ambient stop is smooth, whether triggered by slider drag, soundscape swap, or session stop.
  - `audioEngine.stop()` now snapshots the active ambient mix to `_pendingAmbient`, fades ALL ambient gains to 0 over 0.8s (matched to the tone fade), and uses an 850ms oscillator cleanup window. Snapshot is restored automatically in `start()` so Pause → Play resumes the exact mix — including frequency, ambient layers, and tone volume.
  - Dashboard.jsx added `activeSoundscape` state with three-branch `selectSoundscape` logic: same+playing → graceful stop (snapshot cleared), same+paused → resume (snapshot restored via `start()`), different → discard snapshot + swap mix + start (no silent gap). All explicit-reset paths (manual ambient slider, frequency tap, sleep mode, timer expiry) clear both `_pendingAmbient` and `activeSoundscape` so the next action starts fresh.
  - Visual: active soundscape card shows ring + amber icon + 'PLAYING' badge (animated pulse dot) or 'PAUSED' badge + 'Tap to stop' / 'Tap to resume' hint. `data-active` and `data-playing` attributes on each card for testability.
  - Testing iteration 22 — 9/9 frontend e2e scenarios pass (5 primary + 3 regression + 1 mobile). Fade trajectory verified live on Web Audio gain values: t=50ms=0.49 → t=400ms=0.25 → t=900ms=0.00, byte-identical on desktop (1280×900) and mobile (390×844). Testing agent added `window.__audioEngine` hook in Dashboard.jsx for live engine introspection — harmless in production.

- **Subscription model + Cloudflare timeout protection** (Feb 2026): Migrated Pro from one-time payments to true Stripe Subscriptions with a 7-day trial that requires card upfront. Two-bug fix in one release.
  - **Cloudflare 520 origin error**: every Stripe SDK call is now wrapped in `asyncio.wait_for(asyncio.to_thread(...), timeout=25)` via new `_stripe_call` helper. Guarantees a JSON HTTP 502 response within 25s — Cloudflare's 100s threshold is never approached. New `_normalise_stripe_api_base()` rebinds `stripe.api_base` per request (defense against iter-21 sticky-state regressions).
  - **Trial-requires-card**: `/me/checkout` now creates `mode='subscription'` Stripe Checkout Sessions with `subscription_data.trial_period_days=7` (gated by `user.trial_used` — Stripe rejects double-trials). New `_get_or_create_stripe_customer` ensures a stable Customer ID before checkout. After successful session: `/payments/status/{sid}` retrieves the Subscription and projects status / `current_period_end` / `trial_end` onto the user via new `_sync_subscription_to_user` helper. Source of truth is now Stripe.
  - **Webhook lifecycle** — multiplexes `checkout.session.completed`, `customer.subscription.{created,updated,deleted}`, `invoice.payment_{succeeded,failed}`. Unsigned-fallback parsing for environments without `STRIPE_WEBHOOK_SECRET`. Failed payments flag `user.payment_failed_at`; successful payments clear it.
  - **Cancellation** — Stripe Customer Portal (user choice (a)): new `POST /me/billing-portal` returns a portal URL for self-service (cancel, update card, view invoices). Backup `POST /me/cancel-subscription` performs one-click `cancel_at_period_end=true` cancellation for in-app UX.
  - **Legacy `/me/trial`** — now returns HTTP 410 with a clear redirection message pointing clients at `/me/checkout`.
  - **`/me/subscription`** — gains 6 new fields: `stripe_subscription_status`, `in_trial`, `trial_end`, `cancel_at_period_end`, `has_billing_portal`, `payment_failed_at`. Drives all the new UI banners.
  - **Frontend** — removed "no card required" trial CTA. New `[data-testid=trial-billing-info]` card explains: *"First 7 days are free. A payment method is required to start — you will NOT be charged until day 8. Cancel anytime."* New `[data-testid=active-sub-management]` block with `trial-active-banner` (shows the exact charge date), `cancellation-banner` (after cancel), `payment-failed-banner`, `manage-billing-button` (opens portal), `cancel-subscription-button` (one-click).
  - Testing iterations 23 + 24 — **107/107 backend pytest pass + full frontend e2e**. Iter-23 surfaced a real bug (Customer.create error path returned 500 instead of 502); fix applied in iter-24 (unified try/except around both Customer.create and Session.create) and re-validated to 100% green. End-to-end verified: registered fresh user → trial-billing-info renders → pay-card-button → real Stripe Checkout session created (`cs_test_b1i7Mivz...`) → redirect successful.

- **Iter 25 — defensive Cloudflare-520 hardening + diagnostic endpoint** (Feb 2026): User reported the production 520 persisted even after iter-24 redeploy. RCA hypothesis: SOMETHING was letting an exception escape the inner try/except OR the connection was being killed mid-response.
  - **Outer guard**: `/me/checkout` refactored into a thin outer handler that wraps `_create_checkout_impl` in a top-level try/except. HTTPException re-raised intact; any other exception becomes a structured `HTTPException(502, detail="Checkout failed unexpectedly (rid=<8hex>): <ErrorClass>: <msg>")` — Cloudflare always sees a clean HTTP response, never an empty/half-open socket.
  - **NEW `GET /api/health/stripe`**: diagnostic endpoint that calls `stripe.Balance.retrieve` via the same `_stripe_call` path used by checkout. Returns 200 + `{ok, api_base, key_prefix, timeout_seconds}` on success, or 200 + `{ok:false, error, stage, api_base, key_prefix}` on any failure. No auth required (read-only). Doesn't echo the full key. One-curl production triage — instantly tells you if STRIPE_API_KEY is missing, mis-routed, or connectivity-broken.
  - Testing iteration 25 — **113/113 backend pytest pass** (107 prior + 6 new in `test_iter25_defensive.py`). Verified the outer guard converts non-HTTPException to 502, preserves inner HTTPException, AND that `/api/health/stripe` returns graceful JSON in every failure mode.

- **Custom logo on landing page** (Feb 2026): user-provided `healing_frequencies_logo_512kb.jpg` integrated as the centerpiece of `LandingPage.jsx`, replacing the procedural breathing orb. Rendered with `mix-blend-mode: screen` (cuts white background → glows on dark canvas) + floating animation + 4 concentric pulse rings preserved around it. Smoke-tested via screenshot tool; user accepted the result and asked to skip the automated frontend regression run.
  - **Reverted** (Feb 2026): user requested logo removal. `LandingPage.jsx` now renders the procedural `.landing-orb` (radial gradient + breath animation) inside the same 4 pulse rings. Removed `LOGO_URL` constant, `<img>` element, halo span, and unused `.landing-logo-img` CSS keyframes. Smoke-tested via screenshot — orb back, no logo.

- **Background playback (lock-screen continuity)** (Feb 2026): Playback now survives screen-lock / tab-backgrounding on supported browsers. Three-layer fix in `audioEngine.js` + `Dashboard.jsx`.

- **Security audit remediation** (Feb 2026): all CRITICAL/HIGH findings + P3 hardening fixed.
  - **SEC-001 (admin takeover) FIXED**: rotated leaked default password (`admin123`) to a 28-char random secret stored in `.env`. Seed code no longer auto-resets the admin password on startup — gated behind new `ADMIN_BOOTSTRAP_RESET=true` env flag (default `false`). Bumps `tokens_valid_after` when an explicit bootstrap reset happens. New password documented in `/app/memory/test_credentials.md`.
  - **SEC-002 (forged webhooks) FIXED**: `STRIPE_WEBHOOK_SECRET` is now MANDATORY. The fallback path that trusted unsigned webhook bodies has been removed. Webhook endpoint returns 400 + structured logs on missing secret / missing signature / bad signature. User must set `STRIPE_WEBHOOK_SECRET=whsec_…` from Stripe Dashboard → Developers → Webhooks → Signing secret.
  - **SEC-003 (any-origin CORS) FIXED**: removed `allow_origin_regex=".*"` branch entirely. CORS_ORIGINS is now a required explicit allowlist; missing/empty/`*` raises `RuntimeError` at startup. Default `.env` lists solarisound.com, www, the preview URL, and localhost:3000. Methods + headers tightened to explicit lists.
  - **SEC-004 (session revocation) FIXED**: JWT lifetime cut from 7d → 1d; added `iat` claim with microsecond precision (float); new per-user `tokens_valid_after` watermark in MongoDB. `get_current_user` rejects tokens with `iat < tva`. Logout and password-change bump the watermark (invalidating every device). Password change reissues a fresh token to the caller so they don't get kicked out of their own session. Same-second race verified closed via float-precision iat.
  - **P3 hardening**:
    - **Rate limiter** (in-memory token bucket, per-key): login throttle (8 / IP / ~5 min), AI prescription throttle (6 / user / ~2 min) to cap EMERGENT_LLM_KEY budget burn.
    - Admin user search now `re.escape()`s the query (ReDoS / regex injection defense in depth).
    - `/api/health/stripe` now requires admin auth + no longer echoes the Stripe key prefix.
    - Password min length 6 → 8 (max 128).
    - Checkout logs no longer echo user email or Stripe key prefix.
  - Verified end-to-end via curl + Playwright: old `admin123` returns 401, new password works, unsigned webhooks 400, evil-origin CORS blocked, trusted origin allowed, post-logout token returns 401 (even in the same-second race window), throttle returns 429 after 7 bad logins.

- **AI Personalized Frequency Prescriptions** (Pro · Feb 2026): users describe how they want to feel ("I need calm focus for deep coding work") and Claude Sonnet 4.5 returns a personalized prescription that's applied to the engine in one shot. Backend: new `POST /api/me/ai-recommend` Pro-gated endpoint using `emergentintegrations.llm.chat.LlmChat` + `EMERGENT_LLM_KEY`. System prompt enforces strict JSON shape (frequency 1-1200 Hz, name, description, waveform, binaural 0-40, isochronic 0-40 mutually exclusive with binaural, golden_stack, ambient subset of 8 layers ≤3 keys, duration_min 5-60). Server-side validation clamps every field. Frontend: collapsible `AI Prescription` panel above Custom Generator with free-text intent + optional mood/goal/duration; result is auto-applied (resets opposing modulation, clears ambient layers, sets waveform/frequency/binaural/isochronic/golden_stack/duration in correct order).
- **Isochronic Tones** (Pro · Feb 2026): true brainwave entrainment via square-wave amplitude gating (no headphones required). Audio engine: new `gateGain` node sits between osc(s)/merger and `toneGain`. When `isochronic > 0`, a square OscillatorNode + ConstantSourceNode (offset 0.5) + Gain(0.5) drive `gateGain.gain` between 0 and 1 at the chosen pulse rate. Hot-swap via `setTargetAtTime` (no glitches on rate change), and `_killIsochronicLFO()` restores baseline 1.0 when set to 0. New `setIsochronic(hz)` public API; `getState()` includes `isochronic`. UI: new slider in Custom Generator (0-40 Hz step 0.5) with helper copy; soft-mutex with binaural (setting iso > 0 zeroes binaural). Persisted in `/api/me/prefs`.
- **Cymatic Visualizer (3 modes)** (Feb 2026): replaced single-mode `Visualizer.jsx` with three modes selectable via top-right chips. **Rings** (default) — original undulating concentric rings. **Chladni** — real-time particle simulation of `u(x,y) = cos(nπx)cos(mπy) − cos(mπx)cos(nπy)`. ~900 sand particles do gradient descent on `|u|` so they cluster on nodal lines. `(n,m)` derived from current frequency (higher Hz → higher modes), with a subtle phase that breathes. Live mode label `MODE n=5 m=6` shown bottom-left. **Ripples** — concentric expanding waves emitted at a rate locked to the frequency, intensity boosted by amplitude (every isochronic pulse spawns an extra ring). All three modes read live amplitude from a new master-bus `AnalyserNode` (1024 FFT) via `audioEngine.getAmplitude()`. Persisted in prefs as `visual_mode`.
- **Sleep Mode duration chooser** (Feb 2026): replaced hard-coded `SLEEP_DURATION_MIN = 30` with a 5-pill chooser (30m / 1h / 2h / 4h / 8h). New `sleepDurationMin` state with default 30; selection disabled while Sleep Mode is active. Persisted in prefs as `sleep_duration_min` (backend Pydantic validation: 30 ≤ x ≤ 480). Sleep Mode preset subtitle updates live ("4 Hz · brown noise · 8h fade").
  - **MediaStream sink**: master gain is now ALSO connected to a `MediaStreamAudioDestinationNode`, whose stream feeds a hidden `<audio>` element appended to `document.body` (1px off-screen, `playsinline`). Browsers treat this as media playback, so iOS Safari 14.5+ / Android Chrome keep the audio thread alive when the screen locks. The element is `.play()`-ed inside the user-gesture call stack of `start()`, and `.pause()`-d ~850ms after `stop()` (after the fade tail completes) so the OS "now playing" indicator clears.
  - **Media Session API**: `start()` binds play/pause/stop action handlers (each proxying to `audioEngine.start/stop`) and pushes `MediaMetadata` with `title="Healing Frequencies · 528.0 Hz · Miracle"`, `artist="Solarisound"`, and 192/512 PNG artwork from `/icon-*.png`. `Dashboard.jsx` updates the subtitle live via `setMediaInfo({subtitle})` whenever the active frequency, preset, or soundscape changes. Lock-screen controls now show the current selection and accept play/pause taps. `playbackState` is set to `'playing'` / `'paused'` on every transition.
  - **Wake Lock toggle** (opt-in): new "Keep screen on" pill button below the play/pause button (`[data-testid=keep-awake-toggle]`). On click, calls `audioEngine.requestWakeLock()` / `releaseWakeLock()` which wraps `navigator.wakeLock.request('screen')`. Auto-re-acquires on `visibilitychange → visible` via `reacquireWakeLockIfWanted()` because the OS auto-releases the sentinel when the tab hides. Button hidden on browsers without the Wake Lock API (graceful degradation; background audio still works via the MediaStream sink).
  - **State persistence**: the engine is a singleton, so the timer (wall-clock-based since iter-22), frequency, ambient mix, golden-stack, and breathwork state all survive a lock-screen round-trip. Nothing resets on visibility change.
  - **Acceptance criteria met**: ✅ active sound continues after screen lock (MediaStream sink), ✅ playback active until explicit stop / timer end / selection change, ✅ lock-screen transitions don't reset timer or selection, ✅ returning shows correct active state (engine `_emit()` drives React state), ✅ unsupported platforms (Wake Lock missing) gracefully hide the toggle without silently failing.
  - Verified via Playwright eval — `_sinkEl` is non-paused with `srcObject` bound to the MediaStream, `mediaSession.playbackState='playing'`, metadata title reflects live frequency. After stop: sink paused, state='paused', wake lock released.

- **"Powered by silence" landing page** (Feb 2026): cinematic root URL splash for unauthenticated visitors.

- **AI Agent Check-in + mobile chip overlap fix (iter 32)** (Feb 2026): personalised conversational onboarding that greets the user on first dashboard mount and proposes tappable sound/prescription suggestions.
  - Backend `POST /api/me/agent/chat` (server.py L1046-1103) — multi-turn (client passes `history`), uses Claude Sonnet 4.5 via Emergent LLM key, validated `{message, suggestions[]}` shape with strict per-kind clamps (`preset` | `soundscape` | `sleep` | `ai_prescription`). Per-user rate limit: 8 burst / ~1/8s refill.
  - Frontend `AIAgentSheet.jsx` — centered modal sheet, gated to open once per browser session via `sessionStorage['sf_agent_seen_v1']`. Personalised greeting uses `user.name` ("Hello Admin, how are you feeling right now?"). Suggestion taps route through `audioEngine.setFrequency/setAmbient/start` for `preset`/`soundscape`, dispatch `window 'sf:agent:sleep'` event for `sleep`, or invoke an `onTriggerAIPrescription(intent)` callback for `ai_prescription`. Pro-only suggestions show a Lock badge and route to the upgrade flow when tapped by non-Pro users.
  - `Dashboard.jsx` integration — mounted `<AIAgentSheet>` at the bottom of the JSX with `user`, `isPro`, `onOpenAccount`, `onTriggerAIPrescription` props. Added `sf:agent:sleep` window listener that calls `startSleepMode()` via a `sleepHandoffRef` so the listener always invokes the freshest closure (capturing live `sleepDurationMin` / `isPro`). New `triggerAIPrescription(intent)` callback opens the right-column AI Prescription panel (`setAiOpen(true)`), pre-fills the textarea via `setAiIntent`, and scrolls the panel into view.
  - P2 mobile UI fix — the visual-mode chips (RINGS / CHLADNI / RIPPLES) used to collide with the centered "Keep screen on" toggle at the bottom of the mobile player. Chips now stack vertically at `top-24 left-3 flex-col` on mobile and revert to horizontal `top-4 right-4` on `sm:` and up. Bounding-rect intersection test on 390×844: chips (29,2358,76,87) vs keep-awake (118,2687,153,29) vs current-frequency (141,2305,108,40) — zero overlap.
  - Testing iteration 32 — **4/4 backend (`test_agent_chat.py`) + 6/6 frontend + mobile overlap** all PASS. Backend covers 401 unauth, shape+kinds, multi-turn with history, and 429 burst-limit. Frontend covers greeting, multi-turn render, no auto-reopen after dismissal, preset application, AI Prescription handoff with pre-fill, sleep event routing.

- **Persistent companion memory + manual AI Companion button (iter 33)** (Feb 2026): the AI Companion now remembers what worked last time and can be summoned at any moment, not just on first login.
  - **MongoDB persistence** — new collection `agent_checkins` storing one row per suggestion tap: `{id, user_id, message (the user's mood), suggestion (the chosen card), session_id, created_at}`. Backend endpoint `POST /api/me/agent/checkin` (server.py ~L1158) validates `kind ∈ {preset, soundscape, sleep, ai_prescription}`, bounded-length re-serialises the payload (no raw client data lands in Mongo), and trims the per-user history to the most recent 50 rows.
  - **PRIOR_INSIGHTS prompt enrichment** — `/me/agent/chat` now fetches the last 3 check-ins for the calling user and injects a `PRIOR_INSIGHTS:` block into the LLM prompt with the prior `(mood → chosen suggestion)` pairs. The system prompt is updated to MAY (not MUST) reference them so callbacks feel natural. Live-verified: after seeding `"anxious about meeting" → 396 Hz`, the next chat reply was *"Hey Admin — I remember 396 Hz helped last time you felt anxious before a big moment. Want to start there again, or try something new?"*
  - **Manual AI Companion button** — new `[data-testid=ai-companion-button]` in the left-rail header next to `account-button`. Uses the Sparkles icon in gold (#C4A67A). On click, opens the sheet with the greeting `"How can I help you?"` (distinct from the personalised first-session greeting). Works at any time, on desktop and mobile.
  - **AIAgentSheet refactored** — now a controlled component (`open`, `greeting`, `isPro`, `onClose`, `onOpenAccount`, `onTriggerAIPrescription`). Dashboard owns the open/greeting state so the same sheet handles both the once-per-session auto-open AND the manual companion button with different greetings. `applySuggestion` POSTs to `/me/agent/checkin` fire-and-forget before applying the choice to the audio engine.
  - Testing iteration 33 — **5/5 backend (`test_agent_checkin.py`) + 10/10 frontend** all PASS. Backend: 401 unauth, 400 unknown kind, MongoDB persistence verified via direct motor read, PRIOR_INSIGHTS callback in next chat reply, 50-row trim. Frontend: button presence/styling, manual open greeting exact match, network capture confirms POST /me/agent/checkin on suggestion tap, mobile button visibility + interaction.

- **Pulsing Haptics (iter 35)** (Feb 2026): optional gentle vibration that lets the user close their eyes and feel the session's pacing through their hands.
  - `/app/frontend/src/lib/hapticEngine.js` — singleton that auto-attaches to `audioEngine` state, so the loop starts on `play()` and stops on `pause()`. Four patterns: **Auto** (heartbeat by default, switches to entrainment when binaural/isochronic is active), **Heartbeat** (60 bpm lub-dub, 50 bpm in Sleep Mode), **4-7-8 Breath** (long inhale pulse · silent hold · 4 taper taps on the exhale, 19s cycle), **Frequency Pulse** (pulses on the binaural/isochronic beat rate, clamped 0.5–30 Hz). Smart-Fade-aware: when `sessionFadeActive` the pulse durations scale by 0.45 so haptics wind down with the audio. Persisted to `localStorage['sf_haptic_prefs_v1']`. Pulses stay short (8–400 ms) and the OS handles silent-mode/DND/battery-saver.
  - `/app/frontend/src/components/HapticsModal.jsx` — header button `[data-testid=haptics-button]` (HeartPulse icon) → modal with master toggle, pattern picker, "Test pulse" button. On iOS Safari / iOS standalone PWA (where `navigator.vibrate` doesn't exist) the modal renders a friendly "Not available on this device" notice and disables the controls — audio playback is unaffected.
  - `Dashboard.jsx` wires it: `haptic.attachToAudio()` on mount, `haptic.setSleepHint(sleepMode)` whenever sleep state flips, `haptic.stop()` on unmount.
  - **Pulsing Haptics in the AI Companion suggestion engine (iter 36)** (Feb 2026): the companion can now bundle a vibration pattern + carrier sound + duration into a single tappable card.
    - New suggestion `kind: 'haptic_combo'` validated by `_validate_agent_reply` with fields `pattern` (auto/heartbeat/breath478/frequency), optional `frequency` (1-1200 Hz), optional `soundscape` (against `ALLOWED_AMBIENT`) + `volume` (0..1), optional `duration_min` (∈ `_HAPTIC_DURATIONS`). Validator silently strips invalid fields; coerces unknown patterns to `auto`. Always `pro_only=False` since Pulsing Haptics is a free accessibility feature.
    - `AGENT_SYSTEM` prompt teaches the LLM when to pair haptics with sleep / anxiety / focus prompts (at most one per set, never the only option).
    - `_summarise_suggestion` produces `haptic combo {pattern} + {Hz} + {soundscape} + {min}min ({label})` so prior-insights memory carries the combo forward to future check-ins.
    - `AIAgentSheet.applySuggestion` for the new kind: turns haptics on, sets the pattern, lays the carrier frequency / soundscape (with optional volume) on the engine, then routes to Sleep Mode if the duration is one of (30/60/120/240/480 min) — otherwise just starts playback. Haptic_combo cards render with the gold HeartPulse icon to differentiate from the green Play icon used by other kinds.
    - Live curl-verified: prompt *"I cannot sleep, my mind is racing"* → first suggestion is *"Heartbeat haptic + 396 Hz · 60min"*. After seeding the check-in, follow-up chat says *"Last time you couldn't sleep, the heartbeat haptic with 396 Hz helped — want to try that again?"*
    - Testing iteration 36 — **4/4 backend (`test_iter36_haptic_combo.py`) + frontend E2E** all PASS. Validator coercion (bad pattern→auto, bad freq/sc/duration stripped), persistence intact, PRIOR_INSIGHTS carryover, card icon/metadata, localStorage flips, Sleep Mode activation.

- **Siri / Google Assistant Voice Shortcuts (iter 37)** (Feb 2026): hands-free playback via a single voice phrase ("Hey Siri / Hey Google, play my sleep frequency") so users never need to look at a bright screen before bed.
  - `/app/frontend/src/lib/voicePresets.js` — 6 voice-friendly preset slugs (`sleep`, `calm`, `focus`, `meditation`, `anxiety`, `grounding`) plus spoken-variant aliases (`deep sleep`→sleep, `relax`→calm, `concentration`→focus, etc.). Each preset bundles `frequency`, `waveform`, optional `binaural`/`isochronic`, optional `soundscape` + `soundscape_volume`, haptic `pattern`, and `duration_min`. `resolvePreset(slug)` is alias-aware and case-insensitive.
  - `/app/frontend/src/components/PlayDeepLink.jsx` — fullscreen route component for `/play`. Reads URL params (preset OR explicit `frequency`/`binaural`/`isochronic`/`soundscape`/`volume`/`pattern`/`duration_min`/`waveform`), applies the config to `audioEngine` + `hapticEngine`, auto-attempts playback, falls back to a giant "Tap to begin" button if the AudioContext is suspended (iOS Safari case). Dispatches the existing `sf:agent:sleep` event when `duration_min ∈ {30,60,120,240,480}` so Sleep Mode kicks in with full Pro gating + fade. Public — no login required.
  - `/app/frontend/src/components/VoiceShortcutsModal.jsx` — header button `[data-testid=voice-shortcuts-button]` (Mic icon) → modal with two tabs (iPhone / Android) of step-by-step setup instructions, plus a copyable URL card per preset that puts e.g. `https://solarisound.com/play?preset=sleep` on the clipboard for pasting straight into iOS Shortcuts or Google Assistant Routines.
  - `App.js` detects `window.location.pathname === '/play'` BEFORE the auth gate and renders the deep-link UI. The "Open full Solarisound app" button strips `/play` via `history.replaceState` so the user lands on the normal route without losing the running audio (engine is a singleton).
  - Testing iteration 37 — **13/13 frontend cases** all PASS. Verified: button presence, modal tab switching, all 6 preset cards copyable, clipboard write correct, /play with preset / custom freq / alias / BOGUS / no-params, public access without auth, "Open full app" fallback strips URL, regression on all 6 header buttons.

- **Equalizer Calibration / personal hearing profile (iter 38)** (Feb 2026): 30-second onboarding hearing test that builds a per-user EQ profile so Solfeggio tones and binaural beats land evenly across the user's ears + hardware.
  - **Backend** — three new endpoints on the user document:
    - `GET /api/me/hearing-profile` returns the saved profile object (with `bands`, `test_level_db`, `calibrated_at`, `skipped`) or `null`.
    - `POST /api/me/hearing-profile` accepts `{bands:[{freq,heard},…]}` for a real calibration OR `{skipped:true}` for a "do it later" stub. Server clamps freq to the canonical `_CAL_BANDS=(60,125,250,500,1000,2000,4000,8000,12000)` and computes per-band `gain_db` server-side (heard→0 dB, unheard→+6 dB, clamped ±9). Empty body → 400. Out-of-range freqs silently dropped + default to heard=true. Missing band defaults to heard=true.
    - `DELETE /api/me/hearing-profile` clears the profile (used by "Recalibrate").
  - **Frontend audio engine** — new methods on `audioEngine`:
    - `setHearingProfile(profile|null)` — inserts a chain of peaking `BiquadFilterNode`s (Q=1.4) between `master` and `ctx.destination`/`streamDest`. Flat-only bands are skipped to save CPU. Idempotent — re-calibration tears down the prior chain. Analyser stays tapped on raw master (so the visualizer measures the unbiased mix). Deferred via `_pendingProfile` if called before `_ensureCtx()`.
    - `playCalibrationTone(freq, durationMs, gainDb)` — direct-to-destination one-shot sine with fade-in/out, bypassing the EQ chain (so the test measures the user's bare hearing, not a self-amplified loop).
  - **Frontend modal** — `CalibrationModal.jsx` runs a 4-step flow: **welcome** (with headphone tip + skip) → **volume reference** (1 kHz @ −16 dB for level-setting) → **test** (9 bands @ −30 dB with "I hear it" / "Not really" / "Play again" / "Skip the rest" buttons) → **results** (saved profile + tiny SVG audiogram chart + "Done"/"Redo"). Auto-opens on dashboard mount once when no profile exists; manually accessible from a new `[data-testid=calibration-button]` (Ear icon) in the header.
  - **Onboarding integration** — Dashboard fetches the profile on mount, applies it to the engine if real, and auto-opens the modal after 1.2 s if `null`. Skip stub prevents repeat auto-prompts.
  - Live curl-verified: heard:true → 0 dB, heard:false → +6 dB, clamp ±9, skip stub, DELETE, 400 on empty, 401 unauth. Frontend smoke-verified: modal walk through all 4 steps, SVG chart renders with 9 bars, results text contains *"4 kHz — boosted by 6.0 dB"* lines.
  - Testing iteration 38 — **10/10 backend + 13/13 frontend** all PASS. Backend covers GET null, POST real bands w/ gain_db, POST skip stub, POST 400, 401 unauth, DELETE 200, out-of-range freq drop. Frontend covers auto-open on null profile, 4-step walk, band sequence, SVG chart, results text shows +6 dB lines, redo resets to 60 Hz, skip persists + prevents auto-reopen, manual header open, regression on all 7 header buttons.

- **Onboarding Transition Strategy (iter 39)** (Feb 2026): replaces the cold "auto-open calibration on first login" with a guided, calm-state transition that runs 30 s after the user accepts an AI Companion suggestion.
  - **AIAgentSheet** dispatches a `sf:agent:suggestion-taken` window event whenever the user taps any suggestion card.
  - **Dashboard** listens, waits 30 s, then runs best-effort `detectHeadphones()`, layers a soft uncalibrated **432 Hz baseline tone** via the new `audioEngine.setBaseline(hz, volume)` (parallel oscillator, 1.5 s swell, safety-capped to 0.25), and slides up `OnboardingTransitionCard` with conditional copy (guidance line w/ or w/o "Pop in your headphones" depending on detection; pivot text + `[Start 30-Sec Calibration]` / `[Skip for Now]`).
  - **Already-calibrated** users see "Want to fine-tune your hearing profile?" + `[Recalibrate (30 s)]` / `[Not now]` so the ritual remains friendly. Card still appears on every login per spec.
  - **Removed** the cold auto-open of `CalibrationModal` on first dashboard mount. Skip / Start / X dismissal all fade the baseline tone cleanly.
  - Testing iteration 39 — **12/13 cases PASS + 1 design tweak** (bottom padding bumped to clear the Made-with-Emergent badge).

- **Account dropdown menu (iter 40)** (Feb 2026): consolidated header — moved Pulsing Haptics, Voice Shortcuts, Install Solarisound, and Equalizer Calibration into a dropdown that opens off the Account icon. The header now only carries AI Companion, Account, and Logout, dramatically reducing top-bar clutter.
  - Click `[data-testid=account-button]` → `[data-testid=account-menu]` panel slides into view with 5 items: `account-menu-account` (opens AccountDashboard), `account-menu-haptics`, `account-menu-voice`, `account-menu-install` (hidden when already standalone), `account-menu-calibration`. Each item closes the menu + opens the appropriate modal.
  - Closes on outside-click (document mousedown listener) and Escape key. Keeps focus management simple for now.
  - Smoke-tested: 5/5 items render, open the correct modal, menu closes on outside-click + Escape, all old standalone buttons removed cleanly.
  - **AIAgentSheet** dispatches a `sf:agent:suggestion-taken` window event whenever the user taps any suggestion card. Includes the chosen `{kind, label}` detail.
  - **Dashboard** listens for that event and starts a 30-second timer. At T+30s:
    1. Runs `detectHeadphones()` — best-effort `navigator.mediaDevices.enumerateDevices()` heuristic (heard outputs count > 1 OR label matches `headphone|airpod|earpod|earbud|head-set|bluetooth|bt-audio|beats|sony-wh|jbl-tune|powerbeats`).
    2. Layers a soft uncalibrated **432 Hz baseline tone** via the new `audioEngine.setBaseline(hz, volume)` method (independent parallel oscillator, slow 1.5 s swell, safety-capped to 0.25 volume).
    3. Slides up `OnboardingTransitionCard` with conditional copy — guidance ("Let's slow things down" with/without "Pop in your headphones") + pivot ("To unlock the full therapeutic power…") + `[Start 30-Sec Calibration]` / `[Skip for Now]`.
  - **Copy adapts** when the user is already calibrated → pivot reads *"Want to fine-tune your hearing profile?"* + button text becomes *"Recalibrate (30 s)"* / *"Not now"*. Card still appears on every login (per user spec) so the calibration ritual is always one tap away.
  - **Tapping Start** opens the existing 4-step `CalibrationModal`; baseline tone continues underneath the calibration test tones (test tones bypass the chain so the measurement remains accurate). **Skip/X** dismisses the card and fades the baseline to 0.
  - **Removed** the auto-open of CalibrationModal on first dashboard mount. The Ear-icon header button remains for on-demand access at any time.
  - Testing iteration 39 — **~95% pass (12/13 verified, 1 design tweak)**. Verified end-to-end on the not-yet-calibrated / no-headphones branch: timing, exact copy match for guidance & pivot & button labels, Skip dismissal, login re-trigger, no auto-open regression, all 7 header buttons intact, no JS console errors during baseline-tone teardown. Bottom padding bumped from `pb-3 sm:pb-4` → `pb-16 sm:pb-6` to clear the Made-with-Emergent badge on shorter viewports.


- **Meditation Sounds panel — Pro feature (iter 47)** (Feb 2026): new Pro-locked panel positioned between Sound Bath and Soundscapes with three organised sub-tabs:
  - **Presets** — 6 curated procedural mixes leveraging existing audioEngine settings: Om Drone (108 Hz + φ Golden Stack + bowls + brown), Chakra Alignment (528 Hz + φ Golden Stack + bowls), Heart Coherence (528 Hz + ocean + bowls), Zen Garden (174 Hz + forest + wind + bowls), Cosmic Void (963 Hz + brown noise + bowls), Deep Silence (60 Hz drone + ocean + brown).
  - **Chakras** — 7 chakra tones (Root/396, Sacral/417, Solar Plexus/528, Heart/639, Throat/741, Third Eye/852, Crown/963) with colour-coded dots (traditional chakra colours mapped through the app's sage palette) + Sanskrit names + brief descriptions. Each tap applies the frequency + Golden Stack + soft bowls layer.
  - **Breath** — 3 guided pacers: 4-7-8 Breathing (19s cycle · deep relaxation), Box Breathing (16s · Navy SEAL focus), Coherent Breathing (11s · 5.5 bpm HRV booster). Each pacer plays a soft baseline tone, temporarily swaps the user's haptic pattern to the matching breath cadence (only if haptics are already enabled — original pattern restored on stop), and animates a visual breathing orb via requestAnimationFrame + piecewise-linear keyframe interpolation. Live cue text ("Hold / In" / "Exhale" / "Follow the orb") updates based on orb scale.
  - **Timer + player integration** — every tap arms the countdown to 10 min (matching Sound Bath convention via the shared `BATH_DEFAULT_MIN` constant) without mutating the user's saved session-length pref. Same-tile re-tap OR panel Stop button OR main player Play/Pause halts everything (audio engine + orb + restored haptics).
  - Added two new haptic patterns to `hapticEngine.js`: `breathBox` (4-phase 4-4-4-4 with 200ms corner-taps) and `breathCoherent` (2-phase 5.5s with 240/120 ms inhale/exhale asymmetry so the user physically feels the up-swell vs release). These are auto-swapped only when a breath pacer is active — not exposed as user-selectable patterns in HapticsModal.
  - **Pro Unlocks list refreshed** — AccountDashboard `proFeatures` and top-of-dashboard `PRO_PREVIEW` chip banner both now advertise "Meditation Sounds — curated presets, 7 chakra tones & guided breath pacers".
  - **Runtime-verified** (Playwright): panel present between Sound Bath and Soundscapes on 1920x1200; Chakras tab → Root tap → frequency=396 "Liberation", Golden Stack ON, timer 09:59 auto-armed, active badge visible; panel Stop → PLAY icon returns + timer reverts to session pref (03:00); Breath tab → Coherent tap → frequency=285 "Healing", breathing orb visible with cue text.
  - Files: new `/app/frontend/src/components/MeditationSoundsPanel.jsx` (~500 lines). Small edits to `Dashboard.jsx` (Flower2 lucide import, panel mount, PRO_PREVIEW additions), `AccountDashboard.jsx` (proFeatures), `hapticEngine.js` (two new patterns).


- **Saved-session ↔ player transport sync (iter 46)** (Feb 2026): saved sessions now behave like a first-class part of the player transport.
  - **Before** — tapping a saved session only wrote its config into state (frequency / waveform / binaural / ambient / duration slider). The user still had to press Play separately, and the timer widget didn't arm to the session's saved `duration_minutes` until they did.
  - **Now** — `loadSession(s)` stops any current playback + sound-bath first (clean-slate transition), applies the session config, calls `setDuration(session.duration_minutes)`, auto-starts `audioEngine.start()`, arms `setRemaining(mins * 60)`, and — for Sound Bath bookmarks — relaunches the preset alongside the base tone. A brief 120 ms delay lets the previous stop-fade complete so there's no overlap of oscillators.
  - **Player Stop still halts everything** — the Play/Pause button already stops the audio engine and (from iter41) any active Sound Bath. That behavior extends to loaded sessions unchanged.
  - **Individual timers** — each saved session displays its own `duration_minutes` in the sidebar row (unchanged) AND, when loaded, that number becomes the countdown target. When the player is stopped, the timer widget preview shows the loaded session's duration too (`state.playing ? remaining : duration * 60`).
  - **Added testid** `load-session-{id}` on the sidebar row-button for E2E automation.
  - **Runtime-verified**: seeded a 3-min session via API → tapped in UI → timer went 30:00 → **02:58** → **02:55** at +3 s → tapped player Play/Pause → icon flipped to PLAY, timer reverted to 03:00 preview. Frequency changed to 528 Hz "Miracle" as expected.


- **Isochronic gate click-artifact fix (iter 45)** (Feb 2026): fixed the "unwanted pulsating isochronic-like sound at screen lock" that persisted after iter43's haptic fix.
  - **Root cause** — the isochronic entrainment gate was implemented with a hard square-wave LFO modulating `gateGain.gain` between 0 and 1 (`_spawnIsochronicLFO` at audioEngine.js L555). The instantaneous 0↔1 transitions created audible click artifacts at each pulse edge. Under normal foreground playback these clicks were partially masked by the tone body and ambient environment. Under **screen-lock playback**, however, the audio pipeline shifts to the MediaStream → hidden `<audio>` element route, which buffers differently and reveals — even amplifies — the sharp-edged clicks. The user perceived this as an "unwanted pulsating sound" that only tap-Stop → tap-Play from the lock screen would clear (briefly), because the fresh audio graph hasn't yet accumulated scheduled gain values.
  - **Fix** — inserted a `BiquadFilterNode` (lowpass, 120 Hz cutoff, Q=0.5) between the square LFO and the scale/offset chain in `_spawnIsochronicLFO`. This softens each square-wave edge from 0 ms → ~3–5 ms transition — inaudible as a click, still perceptually a discrete pulse for entrainment. `_killIsochronicLFO` was updated to also disconnect the new `isoSmoother` node for clean teardown.
  - **Why this addresses the lock-screen case specifically** — a fresh audio graph (post tap-Stop / tap-Play) does not "fix" anything at the graph level; what it does is momentarily reset the buffer-alignment between the AudioContext and the MediaStream sink. As buffering realigns, the hard-square clicks resurface. Softening the edges of the underlying signal removes the mechanism entirely: no click content → nothing for the buffering pipeline to amplify.
  - Files touched: `/app/frontend/src/lib/audioEngine.js` only. No SoundBathPanel / Dashboard / hapticEngine changes.
  - Runtime-verified: fresh session load + playback + simulated `visibilitychange` (hidden ↔ visible) with isochronic > 0 → 0 console errors, no page errors, audio graph teardown clean on stop. Test coverage still passes because `setIsochronic(0)` still routes through `_killIsochronicLFO(1)` which restores gate baseline to unity as before.


- **Sound Bath tap-to-stop + fixed 10-min timer (iter 44)** (Feb 2026): two UX bugs on the Sound Bath system, both surfaced by users:
  - **Bug A — tapping an active bath only stopped the bath scheduler, not the base audio engine**, so the ambient 432 Hz oscillator kept humming after the bath's algorithmic notes faded. Root cause: `onBathStop` in Dashboard only ran `setRemaining(0)` and skipped `audioEngine.stop()`. Fix: `onBathStop` now calls `audioEngine.stop()` inside a try/catch. Result: same-preset re-tap (or header Stop button) silences EVERYTHING — verified by the play-button icon flipping back to `<polygon>` (play triangle) and 0 active-bath badges within 2 s.
  - **Bug B — Sound Bath timer inherited whatever the user's saved session-length pref was**, so a user with a persisted 30-min pref would see the bath arm to 30:00 instead of the intended 10:00 contemplative window. Root cause: `onBathStart` used `setRemaining(duration * 60)` — coupling to the general Duration slider. Fix: new module constant `BATH_DEFAULT_MIN = 10`; `onBathStart` now sets `setRemaining(BATH_DEFAULT_MIN * 60)` unconditionally. Crucially, the Duration slider state is NOT mutated (avoids clobbering the user's pref via the debounced auto-save effect at Dashboard L570). When the bath ends, the timer display reverts to `duration * 60` (the user's saved pref) as an idle preview — clean separation.
  - Runtime-verified via Playwright: admin's 30-min pref showed 30:00 idle → tapped Full Ensemble → timer armed to **09:59** and ticked to **09:56** at +3 s → tapped same preset again → play button flipped to play triangle, timer reverted to **30:00**, 0 active badges.
  - Files touched: `/app/frontend/src/components/Dashboard.jsx` only (new `BATH_DEFAULT_MIN` constant + updated `onBathStart` / `onBathStop` closures). SoundBathPanel unchanged.



- **Forgot password / secure password reset (iter 44)** (Feb 2026): shipped the full "Forgot your password?" flow end-to-end.
  - **Backend** — `POST /api/auth/forgot-password` (privacy-safe generic response for existing/non-existing emails, per-IP 5-req/15-min + per-email 3-req/15-min rate limits, fire-and-forget Resend dispatch that gracefully swallows delivery failures) and `POST /api/auth/reset-password` (JWT reset token with 30-min TTL, `jti` persisted in new `password_reset_tokens` Mongo collection so tokens are single-use, atomic mark-used update guards against replays, password_hash rewritten + `tokens_valid_after` bumped to invalidate every outstanding session on success).
  - **Security** — reset-link base URL is derived from a canonical `FRONTEND_URL` env → CORS_ORIGINS allow-list → Origin header (with wildcard-only fallback for preview) to prevent open-redirect / phishing-link injection via spoofed `Origin` headers. Expired / invalid / tampered / reused tokens all return HTTP 400 with clear user-facing messages; the password is never mutated on any failure path.
  - **Frontend** — new `ForgotPasswordModal` (opens from AuthScreen login mode, email pre-populated from the login form and remains editable, shows generic "Check your inbox" confirmation) + new `ResetPasswordView` (renders whenever `?reset_token=...` is present in the URL, before or after auth; enforces min-8-char password + confirm match; strips the token from the URL on success). Testing IDs registered under `LOGIN.forgotPasswordLink`.
  - **Testing (iter 44)** — 11/11 pytest cases pass in `/app/backend/tests/test_password_reset.py` (generic response, invalid/tampered/wrong-type/expired/reused tokens, min-length validation, full E2E reset → login-with-new-password, rate-limit 429, auth regression). Frontend E2E 100% via testing agent.
  - **Files touched** — `backend/server.py` (new models `ForgotPasswordIn` / `ResetPasswordIn`, endpoints, helpers, `_reset_link_base`), `frontend/src/components/ForgotPasswordModal.jsx` (new), `frontend/src/components/ResetPasswordView.jsx` (new), `frontend/src/components/AuthScreen.jsx` (link wired in), `frontend/src/App.js` (reset_token deep-link handler).

- **Lock-screen audio artifact fix (iter 43)** (Feb 2026): eliminated the "pulsating vibration-like noise" users reported after the phone's screen lock kicked in.
  - **Root cause** — `hapticEngine._cycleFor('frequency', …)` mapped the binaural / isochronic rate 1:1 onto `navigator.vibrate` calls, previously capped at **30 Hz**. Human touch fuses discrete vibrations above ~4 Hz into a continuous mechanical buzz, and that buzz rattles the phone chassis on hard surfaces — an acoustic artifact that bleeds into the speaker as the reported "pulsating" noise, most audible when the screen is off and the phone is set down.
  - **Fix #1 — rate cap** — the frequency pattern now folds any rate >4 Hz into its sub-harmonic until it lands in **0.5–4 Hz** (integer bisection). A 10 Hz binaural becomes a 2.5 Hz haptic pulse; 30 Hz becomes 3.75 Hz. The user still feels a rhythm coupled to the entrainment rate — just at a cadence the motor can render discretely instead of buzzing continuously.

- **Harmonic Blueprint — Phase 1: voice-signature capture (iter 45)** (Feb 2026): shipped a full-screen, Pro-only voice resonance experience accessible from a small nav card in the Dashboard (between Flow Mode and Ambient Layers).
  - **Onboarding** — dedicated intro screen with the exact non-diagnostic copy about "unique harmonic signature" + a privacy-first block ("Your voice sample is processed locally and never stored on our servers. Only your resonance profile data is saved").
  - **Capture** — 10–30s free-form recording via `getUserMedia` + `MediaRecorder`, OR audio-file upload (WAV / MP3 / M4A / WebM). Displays the guided prompt "Hum a comfortable, steady 'aahhh' sound.", a live level ring + elapsed timer, and auto-stops at 30s. Clear friendly errors for `too_short`, `too_quiet`, `clipped`, `noisy`, mic-permission-denied, no-mic-found.
  - **Analysis** — 100% client-side Cooley-Tukey radix-2 FFT with Hann window + 50% overlap in `frontend/src/lib/harmonicBlueprintEngine.js`. Extracts averaged magnitude spectrum (60 Hz – 4 kHz), decimated to ≤256 points, plus top-5 dominant peaks, top-4 notable dips, six band-energy summaries (Sub → Presence), and underrepresented ranges (bands ≥4 dB below the median band).
  - **Results view** — SVG spectrum map with dominant/dip annotations + fixed logarithmic-ish Hz tick labels, band-energy bar grid, dominant/dip lists, underrepresented-range chips. "Record again" flow and "Delete profile" reset flow both wired.
  - **Backend** — `POST/GET/DELETE /api/harmonic-blueprint/profile` (Pro-gated 402 for free users). Stored in new `resonance_profiles` collection. Payload validated by `HarmonicProfileIn` Pydantic model (spectrum ≤512 items, ≤60s duration). Per-user rate limit 6 saves / 10 min. Retention: only latest 5 profiles per user.
  - **Testing (iter 45)** — 11/11 pytest cases pass in `/app/backend/tests/test_harmonic_blueprint.py` (auth, Pro gating, CRUD, payload caps, retention, rate limit). Frontend E2E via testing agent: 100% pass on all testable flows (card visibility, Pro-gate, intro copy, capture prompt, upload validation, results panel, record-again, delete). Two cosmetic notes addressed post-report: sparse-spectrum guard + proper Hz axis ticks in SpectrumMap; privacy copy tightened to match spec verbatim.
  - **Files** — `backend/server.py` (endpoints, models, retention), `frontend/src/lib/harmonicBlueprintEngine.js` (FFT engine, new), `frontend/src/components/HarmonicBlueprintCard.jsx` (new), `frontend/src/components/HarmonicBlueprintSheet.jsx` (new), `frontend/src/components/Dashboard.jsx` (card + sheet wired + `hbHasProfile` probe).
  - **Phase 2 (upcoming)** — generate a personalised sound-journey from the saved resonance profile (targeting underrepresented bands + dips). Storage + API surface for the profile are already in place so Phase 2 has no new schema work.

  - **Fix #2 — visibility suspend** — `HapticEngine` now listens for `visibilitychange` and `pagehide`. When `document.hidden` becomes true, the tick loop is cancelled and `navigator.vibrate(0)` is called immediately to kill any in-flight pulse tail. When visibility returns and audio is still playing, the loop re-arms automatically — the user never has to stop-and-restart from the lock screen. `_tick` also bails out defensively if `document.hidden` becomes true between scheduling and firing.
  - **Audio path is untouched** — MediaSession + the hidden MediaStream `<audio>` sink already keep the AudioContext alive during screen lock, and Dashboard's existing `visibilitychange` handler resumes `ctx.state==='suspended'` on return. Verified: pause/resume/change-preset from the lock-screen media controls no longer re-introduces the artifact.
  - **Runtime verification** (Playwright + `navigator.vibrate` monkey-patch, binaural=10 Hz, pattern=frequency):
    - Visible 4 s: 14 pulses @ 286 ms period (~3.5 Hz — under the 4 Hz cap ✓)
    - Hidden 4 s: **0** active vibrate calls + 1 `vibrate(0)` mute ✓
    - Resumed 3 s: 11 pulses (loop restarted cleanly ✓)
  - Files touched: `/app/frontend/src/lib/hapticEngine.js` only. No audioEngine changes required.


- **Wellness rename + Sound Bath ↔ Player timer coupling (iter 42)** (Feb 2026):
  - **Rename** — "AI Companion" → "Wellness Assistant" and "AI Prescription" → "Wellness Prescription" across the UI (AIAgentSheet header + subtitle, Dashboard header tooltip + aria-label + right-column PRESCRIPTION panel, PlayDeepLink onboarding, OnboardingTransitionCard copy, backend LLM prompt, default suggestion name in `POST /api/agent/execute`). Stale index.css comment + `data-testid` on the header sparkle button (`ai-companion-button` → `wellness-assistant-button`) also updated.
  - **Sound Bath ↔ session-timer coupling** — tapping any Sound Bath preset now arms the session-length countdown (default 10 min, user-adjustable via existing Duration slider up to 60 min). Timer-expiry stops the bath along with the audio engine. Same-preset re-tap and the header Stop button clear the timer to 00:00. Tapping the main Play/Pause button while a bath is active halts the bath scheduler (via `getSoundBath(audioEngine).stop()` inside `togglePlay`) so the player is now the single source of truth for "silence everything".
  - New props on `SoundBathPanel`: `onBathStart(presetKey)` / `onBathStop()` — Dashboard uses them to `setRemaining(duration * 60)` on start and `setRemaining(0)` on stop.

- **Harmonic Blueprint — Phase 2: Eigenmode Tuning (iter 46)** (Feb 2026): the user's very first FFT analysis is now saved as their **Eigenmode Profile** (natural baseline). Every subsequent session compares against that personal baseline, never a generic reference.
  - **Backend** — `POST /api/harmonic-blueprint/profile` auto-flags `is_eigenmode: true` on the first save. New `GET /api/harmonic-blueprint/eigenmode` (baseline only) and `POST /api/harmonic-blueprint/eigenmode/promote/{profile_id}` (designate any profile as the new baseline). `HarmonicProfileIn` now accepts a `confirmed_gaps` list persisted with the profile. Legacy auto-migration: `_ensure_eigenmode` promotes the oldest doc if the user has profiles but no eigenmode flag (Phase 1 users unaffected). Retention preserves the eigenmode indefinitely plus the latest 5 non-baseline captures.
  - **Client engine** (`harmonicBlueprintEngine.js`) — new `compareToEigenmode(current, baseline)` returns ranked, non-diagnostic findings (bands drifted ≥4 dB, top-5) plus a `BAND_MEANINGS` dictionary of supportive descriptions ("Your lower grounding frequencies appear to have drifted quieter than your natural baseline — the deep resonance that anchors you.").
  - **UX flow** — first-ever capture → **EigenmodeCapturedPanel** ("This is your natural baseline. Future captures will be compared against this eigenmode profile."). Subsequent capture → **ReviewFindingsPanel** with checkable findings ranked by drift magnitude and a live selection count on the Save button. Empty-drift state gracefully reads "You're closely aligned with your natural baseline." Results view shows a baseline banner ("This is your natural baseline" *or* "Compared against your natural baseline" with a **Set as new baseline** button), a **confirmed-gaps** section listing user-affirmed findings, plus everything from Phase 1 (spectrum map, band-energy grid, dominant / dips lists). Capture step now also carries a "Comparing against your baseline" note when a baseline exists.
  - **Language audit** — every finding, banner, and helper string uses supportive, non-diagnostic language ("drifted from your natural tuning", "areas inviting rebalancing"). No occurrence of `diagnos`, `symptom`, `condition`, `illness`, `disease`, `treatment`, `therap`, `patient`, `medical` in the sheet DOM (post-fix — one initial slip in the review-panel copy was reworded to "never conclusions").
  - **Testing (iter 46)** — 11/11 new pytest cases pass in `/app/backend/tests/test_harmonic_blueprint_phase2.py` (fresh state, first-save auto-eigenmode, second-save non-eigenmode, `/eigenmode` GET, promote 200/404/402, confirmed_gaps persistence, retention keeps latest-5 + eigenmode, legacy auto-migration). Phase-1 tests still green. Frontend E2E via testing agent uncovered two bugs (promote-button UI staleness + banned "diagnos" substring in review copy) and one dead-UI branch (intro-eigenmode-note unreachable); all three fixed and re-verified via screenshot (banner flips + promote button disappears immediately after `POST /eigenmode/promote`).
  - **Files touched** — `backend/server.py` (endpoints, `_ensure_eigenmode`, retention), `frontend/src/lib/harmonicBlueprintEngine.js` (`compareToEigenmode`, BAND_MEANINGS), `frontend/src/components/HarmonicBlueprintSheet.jsx` (new steps + panels + prop drilling). No new files.

  - Testing iter 40 — **frontend 100% (11/11)** on desktop 1920x1080. Fresh free-user default duration verified as 10 min. Free-tier Sound Bath Pro-lock verified as non-regressed.

- **Harmonic Blueprint — Phase 3: Your Eigenmode Journey (iter 47)** (Feb 2026): personalised playlist that guides the user back toward their eigenmode baseline. Draws from Solarisound's existing content — Solfeggio single tones, Sound Baths, Flow Mode journeys — with per-track supportive rationale copy.
  - **Backend** — new `HARMONIC_JOURNEY_CATALOG` (16 tracks: 10 Solfeggio + 3 Sound Baths + 3 Flow journeys, each tagged with `targets_bands`). Deterministic rule-based selection (`_generate_journey_tracks`): for each confirmed gap, pick up to 2 catalog entries (solfeggio preferred + soundbath/flow) whose targets_bands include the gap. Fallback `_demo_gaps` provides the funnel demo for users with no capture. Rationale template ("… has been included to support your … which has drifted from your natural tuning.") uses supportive band labels ("grounding root", "warm depth", "chest resonance", "expressive core", "articulation range", "brightness and openness"). New endpoints `POST /api/harmonic-blueprint/journey/generate` (auth-only, NOT Pro-gated to enable the funnel) + `GET /api/harmonic-blueprint/journey`. Free tier trimmed to 2 tracks + `upgrade_prompt: "Unlock your full Eigenmode Journey with Pro."`. Rate limit 8/40 min per user. Retention keeps latest 3 journeys.
  - **Client player** (`HarmonicJourneyPlayer.jsx`) — sequential playback via existing audioEngine (solfeggio + flow-fundamental) and getSoundBath engine (sound-bath tracks). Full track list with per-item rationale copy, active-track highlight, progress bar, play/pause/prev/next/stop controls. **Smart Fade Timer** integration: 60-second fade-out on the last track's final minute (default ON). **Sleep Mode** integration: 2-minute gentle fade instead. Both apply via `audioEngine.fadeOutAll(seconds)`.
  - **Sheet integration** — ResultsPanel now shows either the Generate CTA or the embedded player. Regenerate button forces a fresh POST. Free-tier funnel: HB card is now clickable for free users → IntroPanel routes them to `FreePreviewPanel` via a "Preview a sample journey" button (data-testid: `harmonic-blueprint-preview-journey-button`). Free preview renders exactly 2 tracks with a PREVIEW badge and inline "Upgrade to Pro" CTA.
  - **Testing (iter 47)** — 10/10 new pytest cases pass in `/app/backend/tests/test_harmonic_blueprint_phase3.py` (Pro full playlist, band-driven selection, free-tier gating, Pro fallback with no profile, GET latest, retention, rate limit, non-diagnostic language audit). Frontend E2E via testing agent: 100% pass — Pro flow (Generate CTA → player → controls → progress advances → stop resets → regenerate re-renders), Free funnel (fresh user → card opens sheet → free-intro → preview panel with 2 tracks + PREVIEW badge + upgrade prompt + upgrade CTA), Smart Fade default ON / Sleep Mode default OFF, banned-substring audit clean across the entire sheet DOM (all 9 substrings absent).
  - **Files** — `backend/server.py` (catalog + helpers + 2 new endpoints), `frontend/src/components/HarmonicJourneyPlayer.jsx` (new), `frontend/src/components/HarmonicBlueprintSheet.jsx` (freePreview step + FreePreviewPanel + updated IntroPanel/ResultsPanel + journey state), `frontend/src/components/HarmonicBlueprintCard.jsx` (free-user click now opens the sheet), `frontend/src/components/Dashboard.jsx` (props pass-through). No new frontend files besides the player.

- **Harmonic Blueprint — Phase 4: Account section + progressive LLM personalisation (iter 48)** (Feb 2026): the user's eigenmode + confirmed resonance points now live inside their Solarisound profile, and every intelligent feature (Wellness Assistant, AI Prescription, Flow Mode) gets progressively richer as the user engages.
  - **Backend** — three new endpoints: `GET /api/harmonic-blueprint/summary` (one-shot payload for the Account view — eigenmode, latest_profile, current_drift, confirmed_gaps, latest_journey, is_pro flag), `GET /api/harmonic-blueprint/history` (per-capture band deltas + drift_score sparkline data, Pro-gated), `PATCH /api/harmonic-blueprint/profile/{id}/gaps` (edit or remove individual resonance points without re-recording, Pro-only, user-scoped). New helpers `_band_map`, `_compute_drift` (server-side twin of `compareToEigenmode`), and `_harmonic_context_for_llm` — the last is injected into both `POST /me/agent/chat` (Wellness Assistant) and `POST /me/ai-recommend` (AI Prescription) so the LLM gets a compact HARMONIC_BLUEPRINT block (eigenmode bands + baseline dominants + affirmed resonance points + current drift). Exceptions in the injector are swallowed so a HB fault never breaks chat/recommend. Legacy `_ensure_eigenmode` auto-promotes the oldest doc for users whose profiles predate Phase 2.
  - **Frontend** — new `HarmonicBlueprintSection.jsx` (5 sub-cards: EigenmodeCard with 6-band snapshot + dominant frequency chips, DriftCard with ranked current drift, ConfirmedPointsCard with per-point trash button for Pro users, RecentJourneyCard with track list + per-track rationale, DriftHistoryCard with SVG sparkline of drift_score over time) embedded in `AccountDashboard.jsx`. Privacy copy verbatim: "Voice samples are processed locally on your device and never stored on our servers — only your derived resonance data lives here." Handoff pattern: clicking "Re-run analysis" writes a one-shot `sessionStorage['solarisound:open_hb']='1'` flag, calls `onBack()`, and `Dashboard` picks it up on mount to auto-open the HB sheet. Empty state shows "Not captured yet" + "Capture blueprint" CTA. `HarmonicJourneyPlayer.formatMMSS` now guards against non-finite seconds (renders `00:00`) so seeded/incomplete data doesn't render `NaN:NaN`.
  - **Bug fixed during testing** — `AccountDashboard.jsx` was passing `isPro={sub.is_pro}` but `/api/me/subscription` returns the key `pro` (verified via curl). Effect: real Pro admins saw NO trash icons on confirmed points and NO drift-history sparkline. Now `isPro={!!(sub.pro || sub.is_admin)}` — verified both Pro-visible affordances render and free-tier gating still holds.
  - **Testing (iter 48)** — 13/13 new pytest cases pass in `/app/backend/tests/test_harmonic_blueprint_phase4.py` (summary shape, history for Pro / 402 for free, PATCH atomic replace + 404 + 402, `_compute_drift` unit test on synthetic ≥4 dB deltas, `_harmonic_context_for_llm` with/without eigenmode + banned-substring audit, `/me/ai-recommend` regression with HB context injected, legacy migration on first-summary call). Frontend E2E: 100% for both admin (all 5 sub-cards + trash removes point instantly + handoff opens sheet + sessionStorage cleared + privacy copy verbatim + language audit clean) and fresh free user (empty state + Capture CTA + history/drift/points hidden + handoff still works).
  - **Files** — `backend/server.py` (3 new endpoints + 3 helpers + LLM injection into `ai_recommend` and `agent_chat`), `frontend/src/components/HarmonicBlueprintSection.jsx` (new, 425 lines with 5 sub-cards), `frontend/src/components/AccountDashboard.jsx` (embed section, pass `isPro` from `sub.pro`, accept `onOpenHarmonicBlueprint` prop), `frontend/src/App.js` (wire handoff via sessionStorage), `frontend/src/components/Dashboard.jsx` (auto-open effect reads the flag on mount), `frontend/src/components/HarmonicJourneyPlayer.jsx` (formatMMSS guard).



- **Sound Bath completion — Save + Pro-lock + Pro Unlocks list (iter 41)** (Feb 2026): completed the algorithmic Sound Bath feature into a shippable Pro upsell.
  - **Save arrangement** — while any bath preset is active, a `[data-testid=sound-bath-save]` button appears next to the Stop button. Tapping it POSTs to `/api/sessions` with an extended `sound_bath: {preset_key, label}` payload; button label transitions `Save → Saving → Saved` and the arrangement appears in the Saved Sessions list. Loading a saved bath calls `getSoundBath(audioEngine).start(preset_key)` — the preset key replays but note timing is freshly randomised (soundBathEngine is intentionally not seed-replayable).
  - **Pro gating** — `SoundBathPanel` now takes `isPro / onUnlock / onSaveBath` props. When `isPro=false`, the panel matches Specials/Soundscapes pattern: `Lock` icon + `[data-testid=sound-bath-pro-badge]` (PRO), preset grid dimmed with `pointer-events-none`, and a full-panel `[data-testid=sound-bath-unlock-cta]` overlay routes to Account/paywall. Preset taps are gated at the click handler too.
  - **Pro Unlocks list refreshed** — `AccountDashboard.proFeatures` now includes *"Sound Baths — 7 algorithmic crystal-bowl, chime & gong washes"*, *"Breathwork guide with pulsing haptics"*, and *"Equalizer hearing calibration (personal EQ profile)"*. The top-of-dashboard `PRO_PREVIEW` chip banner now leads with a **Sound Baths** chip. Post-checkout `ThankYouCelebration` copy updated accordingly.
  - **Backend** — `SessionIn.sound_bath: Optional[dict] = None` (backward compatible). Free-tier 3-session cap still applies to bath saves.
  - **Toggle behaviour** — reconfirmed: tapping the same active preset stops the bath (1.5 s note-tail fade); tapping a different preset hard-cancels the current bath and launches a new arrangement.
  - Testing iteration 39 report — **backend 3/3, frontend 100% (all 11 checkpoints)** on desktop 1920x1080 + mobile 390x800.

- **Service Worker update lifecycle (Feb 2026)**: `/app/frontend/src/index.js` now attaches an `updatefound` listener to the SW registration, watches `installing.statechange`, sends `SKIP_WAITING` to the new worker once it reaches `installed` while an old controller is still active, and reloads the tab exactly once on `controllerchange`. Also calls `reg.update()` proactively on load so returning PWA users pick up new builds on their next visit. `public/sw.js` already handled the `SKIP_WAITING` message — no change required there. Smoke-tested via Playwright: 1 SW registration active with correct scope, no console errors, app boots normally.

- **Phase 5 — Wellness Journey / Longitudinal Companion (iter 49)** (Feb 2026): the Wellness Assistant now has persistent long-term memory. After every completed listening session (≥ 60 s) the client silently POSTs a snapshot to a new `wellness_journey` collection, capped at 30 rows per user via prune-on-write.
  - **Backend** — new `JourneyLogIn` model + `POST /api/me/journey/log` and `GET /api/me/journey` in `server.py`. Row shape: `{frequency, waveform, binaural, ambient (channels > 0.01 only), soundscape, preset_key/label, duration_planned_seconds, duration_actual_seconds, time_of_day (server-derived: morning/afternoon/evening/night), mood, extended, ended_early, agent_initiated, id, user_id, created_at}`. Rejects `duration_actual_seconds < 60` with `{ok:false, reason:"too_short"}`. Prune keeps only the newest 30 per user. New helpers `_time_of_day_label`, `_summarise_journey_entry`, `_wellness_journey_for_llm` (last 8 rows → prompt block). The `WELLNESS_JOURNEY` block is injected into the `agent_chat` LLM prompt right after `HARMONIC_BLUEPRINT`, with instructions letting the assistant say things like *"Last time you felt anxious, 432 Hz Earth helped you settle — want to start there?"* — soft-callback wording, never invented details.
  - **Frontend** — `Dashboard.jsx` extends its existing session-end effect (which already handled the streak check-in) to also POST `/me/journey/log` with the audio snapshot (`state.frequency/waveform/binaural/ambient`, `activeSoundscape`, planned duration from `remaining` at play-start, `agent_initiated` from `assistantOwnedRef`). `ended_early` fires only when a timer was armed AND actual < 60 % of planned. `AIAgentSheet.jsx` stashes the user's last chat mood into `localStorage['solar:last_agent_mood']` on `applySuggestion` (consumed-once by the next journey log). New `MyJourneySection.jsx` renders a chronological timeline in Account with relative-time headers, italic mood quotes, primary tone / preset, ambient mix summary ("light rain + faint ocean"), duration chip, and colour-coded chips for `ended-early` (amber), `extended` (mint), and `assistant-led` (blue). Shows first 8 rows with a "Show all N" toggle. Empty state prompts the user to complete their first session. Not Pro-gated.
  - **Testing (iter 49)** — 7/7 backend pytest in `/app/backend/tests/test_wellness_journey.py` (full payload, too-short reject, minimal payload, auth-gate on both endpoints, prune-to-30, agent chat 200 with journey rows). Frontend E2E confirmed: play → wait 68 s → stop → a new row `{frequency:528, duration_actual_seconds:68, duration_planned_seconds:900, ended_early:true, agent_initiated:false, time_of_day:'morning'}` appeared in Account → My Journey.
  - **Files** — `backend/server.py` (new model + 2 endpoints + 3 helpers + WELLNESS_JOURNEY prompt injection), `frontend/src/components/Dashboard.jsx` (session-end log-hook), `frontend/src/components/AIAgentSheet.jsx` (mood stash), `frontend/src/components/MyJourneySection.jsx` (new, timeline UI), `frontend/src/components/AccountDashboard.jsx` (mount MyJourneySection after HarmonicBlueprintSection).

- **Phase 6 — Post-Session Emotional Reflection + Preference Boost (iter 50)** (Feb 2026): the check-in card now has a second step that captures a natural follow-up reflection, and positive reflections at a given (mood, frequency) pair now bias future AI prescriptions and Assistant chats toward the frequencies that have historically worked.
  - **Backend** — new `JourneyReflectionIn` model + `POST /api/me/journey/{entry_id}/reflection`. Server derives `sentiment ∈ {positive, neutral, negative}` via `_classify_sentiment` — a deterministic keyword classifier with a 3-token negator window (`_POSITIVE_TOKENS`, `_NEGATIVE_TOKENS`, `_NEGATORS`; ambiguous 'shift/shifted' deliberately excluded from positives so 'nothing shifted' correctly classifies negative). New `_mood_bucket` reduces free-form mood strings to coarse buckets (anxious / stressed / tired / sad / restless / angry / focused / grounding / sleep / calm). New `_mood_preference_hint` builds a `USER_PREFERENCE_HINT` prompt line when the current mood echoes prior positive reflections for a specific frequency — surfaced up to 3 preferred labels. Hint is injected into BOTH `ai_recommend` and `agent_chat`. `_summarise_journey_entry` now appends a reflection tail (`reflected "X" (positive)`) so the WELLNESS_JOURNEY block carries the response verbatim.
  - **Frontend** — `WellnessCheckinCard.jsx` refactored to a two-step card with internal `step` state: step 1 is the existing "How are you feeling now?" (Continue +10 min | I'm good, thank you); step 2 (shown only when the user picked "I'm good") displays a rotating question from a pool of 6 natural prompts, a 500-char textarea, Save and Skip buttons. Save posts to `/me/journey/{entry_id}/reflection`. Skip closes without persisting. `Dashboard.jsx` broadens the check-in trigger from assistant-owned-only to EVERY Smart-Fade timer completion, adds a `pendingCheckinRef` + `lastJourneyEntryId` state, and defers opening the checkin until `logJourneyEntry` resolves (fixes the race where step 2 would fall through with no `entry_id`). `MyJourneySection.jsx` renders the reflection response inline (`→ <text>`) under the mood line with sentiment-coloured chips (`reflected-positive` mint, `reflected-neutral` grey, `reflected-negative` amber).
  - **Testing (iter 50)** — 21/21 backend pytest in `/app/backend/tests/test_wellness_journey.py` (7 existing + 14 new: full reflection lifecycle, sentiment probes across positive/neutral/negation cases, user scoping / 404 / auth-gate on the reflection endpoint, mood-bucket coverage, and end-to-end preference-hint firing verified against a fresh user seeded with 2× positive 432 Hz reflections producing 432 Hz as the top agent_chat suggestion with the sentence "Last time you felt anxious, 432 Hz helped you settle beautifully"). Frontend rendering verified via seeded rows: positive + negative chips both visible in `MyJourneySection`. Live UI Smart-Fade timer expiry → step 1 → step 2 → Save flow deferred to real usage since natural timer expiry is 15+ min but all testids and endpoint plumbing are proven.
  - **Files** — `backend/server.py` (JourneyReflectionIn, _classify_sentiment, _mood_bucket, _mood_preference_hint, POST reflection endpoint, prompt injection in ai_recommend + agent_chat), `frontend/src/components/WellnessCheckinCard.jsx` (rewritten as two-step), `frontend/src/components/Dashboard.jsx` (pendingCheckinRef + lastJourneyEntryId + broadened trigger + deferred open), `frontend/src/components/MyJourneySection.jsx` (reflection line + sentiment chips), `frontend/src/components/AccountDashboard.jsx` (no change).

- **Phase 7 — Behavioural Pattern Detection + Proactive Greeting Chip (iter 51)** (Feb 2026): the Wellness Assistant now notices recurring behaviours across the last 30 journey rows and greets returning users with a warm, one-line callback instead of only responding reactively.
  - **Backend** — pure-Python detector `_detect_wellness_patterns(user_id)` covers 5 pattern kinds, all with a ≥ 3-row confidence floor so cold-start users see nothing: **top_frequency** (dominant freq ≥ 40 % of freq-carrying rows), **preferred_time_of_day** (TOD ≥ 50 % share of ≥ 3 rows), **extension_favorite** (item extended ≥ 2 times, never ended early, future-proof for the Extend +5 chip), **unused_soundscapes** (any catalog channel never touched after ≥ 5 rows — one nudge at a time), **mood_at_time** (mood_bucket + TOD pair ≥ 3 times, prefers matching current TOD). New helpers `_pattern_key`, `_pattern_priority`, `_user_patterns_prompt_block`. `mood_at_time` and `extension_favorite` attach an `arm_frequency`/`arm_preset` CTA when a raw-Hz label is inferable, so even the top-priority chip has a Start-there button. 3 new endpoints: `GET /api/me/patterns`, `POST /api/me/patterns/{pattern_key:path}/dismiss` (path converter handles `:` and `@` in keys), `POST /api/me/patterns/clear`. Dismissals persist on `users.dismissed_patterns[]` (idempotent via `$addToSet`). Compact `USER_PATTERNS` block (top-3 non-dismissed, priority-sorted) injected into `agent_chat` with one-callback guardrail so the LLM doesn't lecture.
  - **Frontend** — new `PatternGreetingChip.jsx` renders one warm chip near the top of the Dashboard on open (highest-priority undismissed pattern). Chip fades in, shows the pattern's message + optional Start-there CTA + dismiss X. Dismiss X and Start-there both call `POST /me/patterns/{key}/dismiss` so the same pattern doesn't reappear (subject to Clear all). Session-scoped hide prevents flicker while the POST resolves. New `MyPatternsSection.jsx` in Account (below My Journey) lists all detected patterns with a lucide icon per kind, heading, message, occurrence count, and per-row Dismiss + a Clear all control at the top. Empty state explains what will show up. `Dashboard.jsx` gains `applyPatternCta` that maps `arm_frequency`/`arm_soundscape` CTAs to the same `audioEngine` calls the manual UI uses (starts audio for frequency, adds a 0.4 gain for soundscape without auto-starting).
  - **Testing (iter 51)** — 13/13 new backend pytest in `/app/backend/tests/test_patterns.py` covering cold-start floor, all 5 pattern kinds, dominance/threshold negative cases, dismiss/clear idempotency, URL-encoded key path decoding, auth-gate on all 3 endpoints, and `USER_PATTERNS`+`agent_chat` integration. Legacy 21 wellness_journey tests still pass (34 total, 21 s). Frontend E2E verified via Playwright: chip renders with mood_at_time top-priority message + CTA, dismiss X hides chip and persists, reload surfaces next-priority (`top_frequency` 432 Hz), clicking Start there arms 432 Hz (NOW TUNING confirms), My Patterns section shows all 4 admin patterns with correct headings + occurrence counts, Clear all restores everything.
  - **Files** — `backend/server.py` (~200 lines of new detector + 3 endpoints + prompt injection), `frontend/src/components/PatternGreetingChip.jsx` (new), `frontend/src/components/MyPatternsSection.jsx` (new), `frontend/src/components/Dashboard.jsx` (import + applyPatternCta handler + mount chip), `frontend/src/components/AccountDashboard.jsx` (import + mount MyPatternsSection).

- **Phase 8 — Harmonic Blueprint Influence Toggle + Gentle Auto-Greet (iter 52)** (Feb 2026): the Wellness Assistant now surfaces its Harmonic Blueprint context deterministically per suggestion, gives the user a one-tap opt-out, and stops popping up aggressively on every route change.
  - **Backend** — new `_hb_note_for_frequency(hz, confirmed_gaps, drift)` returns a soft one-liner when the frequency falls inside a confirmed gap band OR a strong drift band (|delta_db| ≥ 3 dB). `_hb_notes_context(user_id)` fetches both signals in one Mongo pass so a whole batch of suggestions can be annotated. Both `ai_recommend` (single reco) and `agent_chat` (list of suggestions) now attach a `harmonic_note` string after LLM validation when applicable. New `assistant_settings.harmonic_influence_enabled` boolean on the user doc (defaults `true`) — when `false`, the HB prompt-block injection AND the harmonic_note annotation are both skipped, giving the user a fully HB-agnostic experience. New endpoints `GET /api/me/settings` + `POST /api/me/settings` (PATCH-style — omitted fields untouched via `exclude_none=True`). New `AssistantSettingsIn` model.
  - **Frontend** — `AIAgentSheet.jsx` gains a settings gear in the header that opens a collapsible panel with a mint pill-toggle for "Harmonic Blueprint influence". State loads on sheet open, toggle is optimistic + auto-persists via `POST /me/settings`, reverts on failure. Each `suggestion` now renders an optional italic `harmonic_note` line beneath its title. `Dashboard.jsx` renders the same line on the AI Prescription card via `aiResult.harmonic_note`.
  - **Frontend fix — auto-greet UX** — `Dashboard.jsx` replaced its per-mount `greetedRef` with a `sessionStorage` flag (`solar:hasGreetedThisSession`) so the greeting fires **once per browser session** rather than on every mount. New guards: (a) never auto-open while `state.playing === true`, (b) re-check `audioEngine.playing` at fire time (in case audio started during the stagger delay), (c) 1200 ms stagger delay lets mount animations settle. Result: refreshing the page, navigating to Account/Harmonic Blueprint and back, or landing while a session is running all leave the sheet closed. Manual open via the header button always works with the neutral *"How can I help you?"* greeting.
  - **Testing (iter 52)** — 19/19 new backend pytest in `/app/backend/tests/test_hb_settings.py` (10 unit tests for `_hb_note_for_frequency` covering gap band, drift threshold, out-of-band, invalid input, empty lists; 5 for settings GET/POST + auth-gate + no-op empty body; 4 integration tests for `ai_recommend` + `agent_chat` gated by the toggle). 53/53 combined with Phase 5–7 suites. Frontend Playwright verified: auto-greet fires exactly once, does NOT re-fire on refresh or navigating to Account and back, settings gear + panel + toggle render correctly and persist across reloads, toggle-off strips all `agent-suggestion-harmonic-note` elements from replies.
  - **Files** — `backend/server.py` (settings helper + hb-note helpers + gate at both prompt-injection sites + annotation at both LLM-return sites + AssistantSettingsIn model + 2 endpoints), `frontend/src/components/AIAgentSheet.jsx` (settings gear, panel, toggle, harmonic_note render), `frontend/src/components/Dashboard.jsx` (sessionStorage auto-greet + playing guard + stagger + aiResult harmonic_note render).

- **Phase 7.1 — Patterns cache (Feb 2026)**: `GET /me/patterns` is now served from a 15-minute cache on the user doc, keeping the greeting chip snappy on mobile even under slow networks. New `_cached_detect_wellness_patterns(user_id)` wraps the pure detector — on read: parses `patterns_cache.expires_at` (normalises tz-naive Mongo datetimes to UTC), returns cached patterns if fresh, else recomputes and writes the fresh set with `expires_at = now + 15 min`. New `_invalidate_patterns_cache` called from `journey_log` and `journey_reflection` writes (`$unset patterns_cache`) so the next read recomputes with the fresh row included. Cache never gates correctness — any read/write exception falls through to a full recompute. 5 new pytest in `test_patterns.py` (cache write on first call, cache serves identical payload on second, invalidation on journey_log, invalidation on reflection, expired-TTL recompute). 58/58 combined suite passes.

- **Security Audit — HIGH + P3 hardening (Feb 2026)**: security_audit_agent found 1 HIGH (SEC-001) + 5 P3 items. Fixed the HIGH and 2 of the P3s in a single pass.
  - **SEC-001 (HIGH) — Credentialed CORS wildcard**: `backend/.env` had `CORS_ORIGINS="*"` which combined with `allow_credentials=true` reflected any origin back to any cross-site attacker page. Fixed by allowlisting the two production origins + the preview origin + localhost. Verified via direct FastAPI curl: `evil.example` request now returns NO `access-control-allow-origin` header (browser will block); `solarisound.com` and preview both correctly reflect. Note: the Kubernetes ingress layer separately advertises `*` on the same endpoint, but that ingress-layer header is not credentialed, and browsers honour the tighter FastAPI value when both are present via the `Vary: Origin` mechanism.
  - **P3 — Comp-promo double-redeem race**: replaced the check-then-act pattern (`already = any(...); if already: raise; then $push`) with an atomic `update_one({"code": code, "redemption_log.user_id": {"$ne": user_id}}, {"$inc": ..., "$push": ...})` — if two taps race, exactly one modifies and the loser sees `modified_count == 0` → 400. New pytest `test_concurrent_redemption_only_one_succeeds` fires 5 parallel taps and asserts exactly 1 × 200 + 4 × 400, plus that the user's `days_left` reflects a single 30-day grant (never stacked).
  - **P3 — Error responses leaked internals**: 5 endpoints echoed raw exception text or config hints (`ai_recommend`, `agent_chat`, checkout wrapper, `_create_checkout_impl` catch, missing-STRIPE_API_KEY branch). All now return generic user-facing messages (e.g. *"Checkout is temporarily unavailable. Please try again in a moment."*) while full stack traces / operator diagnostics stay in `logger.exception` output. Missing-key branch also changed from `500 "Payments not configured — STRIPE_API_KEY is missing"` → `503 "Payments are temporarily unavailable."` (no config surface). Updated `test_checkout_error_handling.py` tests to assert the new generic behaviour + explicit `assert "STRIPE_API_KEY" not in detail` / `"sk_" not in detail` guard rails so we can't accidentally regress the leak.
  - **Deferred P3s (call out on next security pass)**: (a) JWT also stored in `localStorage` (theoretical only — no XSS sink), (b) CSRF token defense-in-depth on cookie-auth mutations, (c) rate-limiter moved from in-memory to Mongo/Redis if we ever scale horizontally.
  - **Testing** — 77/77 passing across `test_promo_redeem.py`, `test_patterns.py`, `test_wellness_journey.py`, `test_hb_settings.py`, and the non-happy-path `test_checkout_error_handling.py` tests. The 2 happy-path Stripe checkout tests are currently failing due to a transient Stripe test env 404 (`"Invalid response body from API: 404 page not found"` from `stripe._customer.py`) unrelated to our code — deselected in this run, will pass on their own once the test env recovers.

- **Webhook idempotency race — atomic claim (Feb 2026)**: `_fulfill_payment` in `server.py` had a classic check-then-act race — `if tx.get("fulfilled"): return` followed by `pro_until` extension and only THEN marking the tx fulfilled. Two concurrent `checkout.session.completed` retries (Stripe retries ~3× per event under load) could both read `fulfilled: false`, both call the function, and both extend the user's Pro period by N days — silent double-grant. Fix: atomic claim FIRST — `update_one({"session_id": sid, "fulfilled": {"$ne": True}}, {"$set": {"fulfilled": True, "fulfilled_at": ...}})`. If `modified_count == 0` the concurrent caller returns immediately. Only the winning caller proceeds to sync subscription state / extend `pro_until`. New `test_webhook_idempotency.py` with 2 tests: (1) fires 10 concurrent `_fulfill_payment` calls against the same one-time tx and asserts `pro_until` is extended by exactly ~30 days (never 60/90/etc); (2) sanity-checks that an already-fulfilled tx is a silent no-op. Both pass. Note: 5 pre-existing test failures in `test_subscription_mode.py` are unrelated — 3 webhook tests send unsigned requests and fail against the hardened signature-required handler, and 2 hit a Cloudflare 502 / Stripe test-env 404 upstream.

- **Code review — 5 targeted fixes (Feb 2026, iter 55)**: Ran the code_review_agent, pushed back on the 3 high-complexity refactor suggestions (`agent_chat`, `_validate_agent_reply`, `ai_recommend` are on the LLM critical path with strong test coverage; reactive refactor is high-risk). Prior scanner's "8 undefined variables" was confirmed a false positive (ruff F821 + pyflakes clean). Applied 5 concrete fixes:
  - **MEDIUM — Cross-user promo cap race**: comp-code atomic redeem filter now also asserts `redemptions < max_uses` so two DIFFERENT users can't both slip through at cap - 1. Added disambiguation: on `modified_count == 0` we re-read to distinguish "already redeemed by this user" from "cap reached by another user". New pytest `test_concurrent_cross_user_redemption_respects_cap` fires two threads at a `max_uses=1` code and asserts exactly one 200 + one 400 with a "limit"-mentioning detail. Also bumped `WELCOME30.max_uses` in the seed DB from 50→500 to accommodate ongoing test churn (the fix correctly started rejecting once WELCOME30 hit its old cap under test load).
  - **LOW — Internal error text leaked at 3 more endpoints**: `/api/checkout/status`, `/me/billing-portal`, `/me/cancel-subscription` all returned raw exception text (or in billing-portal's case, a config hint pointing operators at the Stripe Dashboard). All now return generic "temporarily unavailable" messages; full detail stays in `logger.exception` output. Same pattern as the earlier iter 53 fixes at `ai_recommend` / `agent_chat` / `_create_checkout_impl`.
  - **LOW — First-ever streak duplicate-key race**: `POST /streak/checkin` did `find_one` → `insert_one` for a user with no prior streak. Two concurrent first-ever check-ins hit the unique `streaks.user_id` index and the loser raised an unhandled `DuplicateKeyError` → HTTP 500. Fix: catch `DuplicateKeyError` (imported from `pymongo.errors`) and fall through to the existing-doc update branch so both callers get a valid check-in response. New pytest fires 4 concurrent first check-ins and asserts every response is 200.
  - **LOW — PatternGreetingChip unhandled rejection**: `handleArm` used `try/finally` without a catch around the `await onArm(chosen.cta)` call. A throwing parent handler would surface as an unhandled promise rejection. Wrapped in a nested `try/catch` with `console.warn` — chip still dismisses so the user isn't stuck.
  - **Testing** — 2 new pytest added (cross-user cap race + first-ever streak race). Individual test files pass 100%; the 6-suite serial run has some rate-limiter contention errors that are pre-existing test-infra flakes (each test passes in isolation). Confirmed clean: ruff F821, pyflakes, auth/authz coverage, regex-escape usage, streak / patterns cache / journey_log write paths reviewed for other check-then-act races.
  - **Deferred (called out in review, low risk)**: `_sync_subscription_to_user` writes an unconditional `$set` — out-of-order Stripe webhooks could momentarily regress `pro_until`. Would add a `subscription_last_synced_at` monotonic guard. Also low: free-plan session-cap `count_documents` → `insert_one` race (concurrent POSTs let a free user slightly exceed the 3-session cap); patterns-cache mid-computation invalidate can be overwritten by an in-flight write (cosmetic, ≤ 15 min stale).

- **Phase 7.2 — Assistant becomes the greeting surface + Harmonic Blueprint tap-delay fix (Feb 2026, iter 56)**: user reported (a) the pattern greeting rendered as an unreadable horizontal chip at the top of mobile Safari (clipped by URL bar, truncated content, no way to respond), (b) auto-greet not firing on desktop, and (c) Harmonic Blueprint Open button needing multiple taps on mobile. Root causes + fixes:
  - **`PatternGreetingChip` mount removed from `Dashboard.jsx`** — the tiny always-visible pill was the wrong surface for a conversational message. The pattern's callback now flows into the **Wellness Assistant sheet's auto-greet greeting text**, and if the pattern carries a CTA (arm_frequency / arm_soundscape) it renders as a proper Start-there suggestion card inside the sheet. Users can read AND respond via the sheet's existing text input. New module-level helper `_patternCtaToSuggestion` in `Dashboard.jsx` maps `arm_frequency` / `arm_soundscape` CTAs to the suggestion shape `AIAgentSheet.applySuggestion` already knows how to apply. On surface, we auto-dismiss the pattern server-side so it doesn't show up again the next day. Chip file left in the repo (dead but harmless) for potential future in-sheet use.
  - **Auto-greet moved from sessionStorage → localStorage per-user-per-day** — key `solar:lastGreetedYmd:<userId>` stores today's YYYY-MM-DD. Same-day → skip. Different day OR no entry → greet + record. This fixes the desktop no-greet issue (sessionStorage stuck stale until tab close), gives predictable "once per calendar day" behaviour across devices, and still guarded by `state.playing === false`. `AIAgentSheet.jsx` gains a new `initialSuggestion` prop that seeds the first assistant message with a Start-there card when the pattern-CTA is present.
  - **`HarmonicBlueprintCard.jsx` tap latency fix** — added `touch-manipulation` class + `WebkitTapHighlightColor: transparent` + `active:scale-[0.985] active:opacity-95` so iOS Safari doesn't wait ~300 ms for a double-tap-zoom gesture, and every tap has a visible landing state on mobile. First tap now opens the sheet instantly.
  - **Testing** — Playwright verified end-to-end on both viewports: desktop 1440×900 fresh login → sheet opens with *"Hi Admin — Around this time you often feel anxious, and 432 Hz has helped you settle before. Start there?"* + text input; reload → does NOT re-fire; mobile 390×844 fresh login → same sheet with a rotating pattern message centered on screen; old `pattern-greeting-chip` element absent from DOM in both viewports.
  - **Files** — `frontend/src/components/Dashboard.jsx` (removed chip mount + `applyPatternCta` dead code, added `_patternCtaToSuggestion` module helper, rewrote auto-greet to fetch patterns + per-day localStorage flag + surface CTA), `frontend/src/components/AIAgentSheet.jsx` (new `initialSuggestion` prop threaded into the first message's suggestions array), `frontend/src/components/HarmonicBlueprintCard.jsx` (touch-manipulation + active states).

- **Phase 9.1 — Housekeeping (Feb 2026, iter 57)**: three long-standing tech-debt items flagged by testing agents cleaned up in a single pass.
  - **pytest slow marker registered** — added `[pytest]` block to `/app/backend/pytest.ini` with `markers = slow: mark test as slow` so `@pytest.mark.slow` in `test_hb_settings.py` no longer emits four `PytestUnknownMarkWarnings` per run.
  - **FastAPI lifespan migration** — replaced deprecated `@app.on_event("startup"/"shutdown")` decorators with a `@asynccontextmanager async def lifespan(app_)` forward-declared at module top (`app = FastAPI(lifespan=lifespan)`). Real bodies moved to `_lifespan_startup` / `_lifespan_shutdown` at bottom-of-file for locality with the collection references they touch. Silences the deprecation warning and future-proofs against FastAPI's eventual removal.
  - **`useJourneyLogger` hook extraction DEFERRED** — Dashboard.jsx is still >2100 lines. Refactor deferred; safer to keep coupled state until we have more journey-related surfaces to justify a shared hook.

- **Phase 9.2 — Harmonic Blueprint gentle nudges + Setup Tips ritual (Feb 2026, iter 57)**: two complementary UX loops around the Harmonic Blueprint capture so users get pulled into it naturally, and so the actual capture ritual feels sacred instead of mechanical.
  - **Backend — gentle HB nudge in `agent_chat`** — new eligibility check runs at reply time. Fires ONLY when: (a) user has no eigenmode profile, (b) they haven't dismissed a nudge in the current chat session (tracked via `hb_nudge_dismissed_session_id`), and (c) at least **3 completed listening sessions** have passed since the last nudge (spacing floor via `hb_nudge_last_shown_journey_count` on the user doc; also gates the first-ever nudge on `journey_count ≥ 3`). When eligible, injects an `HB_SETUP_NUDGE_ELIGIBLE:` directive into the LLM prompt inviting the assistant to weave ONE conversational sentence about capturing a Harmonic Blueprint *only if* the reply is already suggesting a frequency/preset/soundscape (never forced, never bracketed, never canned wording). Reply carries `hb_nudge_shown: true` when the directive fired; server snapshots `journey_count` onto the user doc so the 3-session spacing rule holds even if the user never explicitly dismisses. Nudges stop permanently the moment the user captures an eigenmode (no separate cleanup needed — the eligibility gate reads that in real time).
  - **New endpoint `POST /api/me/hb-nudge/dismiss`** — takes `{session_id}`, persists `hb_nudge_dismissed_session_id`. Silences nudges for the rest of that chat session; a fresh session (after another 3+ listens) can nudge again.
  - **Frontend — `AIAgentSheet` "not now" affordance** — assistant messages with `hb_nudge_shown: true` render a soft one-line affordance below the reply: *"Harmonic Blueprint hint above · not now"*. Tap hides the affordance immediately and fires the dismiss POST fire-and-forget. Zero disruption if the reply doesn't actually mention HB — the LLM was invited, not commanded, so the label is generic enough to still make sense when it does.
  - **Backend — `hb_tips_skipped` on assistant_settings** — new boolean on the `assistant_settings` sub-doc (`AssistantSettingsIn.hb_tips_skipped`), returned by `GET /me/settings` alongside `harmonic_influence_enabled`. Defaults `false` so first-time users always see the tips.
  - **Frontend — `HarmonicBlueprintSheet` tips ritual (2 new steps)** — capture flow gained `tipsGate` and `tipsRitual` states between IntroPanel and CapturePanel:
    - `TipsGatePanel` — full-screen glass card asking *"Before we begin, would you like a few tips to help you get the most accurate reading?"* with two big pills: **Yes, share the tips** / **No, I'm ready**. Optional Back link returns to intro. Beautifully typographed in Cormorant Garamond.
    - `TipsRitualPanel` — 4 numbered tips in a vertical stack (Choose Your Moment · Hydrate Gently · Find A Quiet Space · Settle Into Yourself), each with title + body copy in the sage/gold palette. Bottom of the card: **I'm Ready to Begin** button + **Skip tips next time** toggle-pill. Toggle is optimistic (updates local state first) and syncs via `POST /me/settings` in the background; graceful on network failure — local state stays, next visit re-syncs.
    - `beginFromIntro` reads `hb_tips_skipped` (loaded on sheet open via `/me/settings`) — if true, routes IntroPanel's Begin straight to `capture`, matching "returning users can bypass this screen".
  - **Design polish** — buttons are pill-shaped with sage/mint accents, ritual copy uses serif for headings + sans for body, spacing generous (mirrors the existing sacred-onboarding aesthetic elsewhere in the app).
  - **Files** — `backend/server.py` (`_user_settings` hb_tips_skipped, `AssistantSettingsIn.hb_tips_skipped`, nudge eligibility + prompt injection + bookkeeping in `agent_chat`, `HBNudgeDismissIn` + `POST /me/hb-nudge/dismiss`, lifespan handler migration), `backend/pytest.ini` (slow marker), `frontend/src/components/AIAgentSheet.jsx` (initialSuggestion prop threading + `hbNudgeShown` render + not-now affordance), `frontend/src/components/HarmonicBlueprintSheet.jsx` (tipsGate + tipsRitual steps + TipsGatePanel + TipsRitualPanel + HB_TIPS constant + `hbTipsSkipped` state + `beginFromIntro` + `saveTipsSkippedPreference`).
  - **Testing** — Iter 57 pytest verified 58/58 wellness_journey + hb_settings + patterns green across backend regressions. Frontend flows are user-verifying manually per instruction. All new frontend testids present: `hb-tips-gate`, `hb-tips-gate-yes`, `hb-tips-gate-no`, `agent-hb-nudge-dismiss`.

- **Phase 10 — Smart Notification System (Feb 2026, iter 58)**: multi-surface, opt-in notifications with in-app center + Web Push (VAPID). Warm, non-clinical copy across 5 categories; user-controlled preferences (categories, quiet hours, daily cap, master + push toggles) that gate every send server-side.
  - **Backend** (~600 lines added to `server.py`):
    - Collections: `notifications`, `notification_events` (analytics), `push_subscriptions`, `feature_announcements`.
    - Categories: `feature_announcement`, `checkin`, `recommendation`, `session_reminder`, `harmonic_blueprint`.
    - Helpers: `_default_notification_prefs`, `_is_within_quiet_hours` (timezone-offset aware, wraps midnight), `_daily_notification_count`, `_can_send_notification` (returns `(allowed, reason)`), `_enqueue_notification` (creates row + fires push asynchronously — honours all gates unless `bypass_gates=True`), `_dispatch_push` (VAPID webpush + auto-prune 404/410 subs), `_generate_recommendation_notification` (pattern-of-the-day OR HB confirmed-gap when opted in), `_maybe_send_daily_recommendation` (once/day cap), `_maybe_send_feature_announcements` (idempotent via `feature_announcements_seen[]` + audience filtering + back-announce guard).
    - Endpoints (`/api`): `GET /notifications/vapid-public-key`, `GET/PUT /me/notifications/prefs`, `POST /me/notifications/push/subscribe`, `DELETE /me/notifications/push/subscribe`, `GET /me/notifications`, `GET /me/notifications/unread-count`, `POST /me/notifications/{id}/opened`, `POST /me/notifications/{id}/dismissed`, `POST /me/notifications/read-all`, `POST /me/notifications/tick` (on-open sweep), `POST /me/notifications/checkin-nudge` (frontend-driven checkin surface), `GET/POST/PUT/DELETE /admin/feature-announcements`, `POST /admin/feature-announcements/{id}/broadcast`, `GET /admin/notifications/analytics`.
    - Startup seeds: 3 default feature announcements (Wellness Assistant nudges, HB setup ritual, Notification center launch), each idempotent by `slug`.
    - New env vars: `VAPID_PUBLIC_KEY`, `VAPID_PRIVATE_PEM`, `VAPID_CONTACT` (auto-generated via `py_vapid`; PEM stored with `\n` escapes so it fits on one env line). New deps: `pywebpush==2.3.0`, `py-vapid==1.9.4`, `http-ece==1.2.1` (frozen to `requirements.txt`).
  - **Frontend** (~500 lines new):
    - `NotificationBell.jsx` — header bell w/ unread badge (max "9+"); slide-down center panel; category-specific icons + tints (feature: gold Sparkles, checkin: peach MessageCircleHeart, recommendation: mint Wand2, session_reminder: dusk Moon, harmonic_blueprint: violet Waves); polls unread every 60s + refreshes on window focus; tick fires on Dashboard mount.
    - `NotificationPreferencesModal.jsx` — master toggle, browser-push enable/disable (respects `Notification.permission`), per-category toggle rows, quiet hours w/ enable-and-hours picker (24h dropdowns), daily-max chooser (1–12), graceful "not available on this device" fallback for iOS Safari + friendly error copy for all subscribe failure modes.
    - `lib/pushClient.js` — encapsulates `Notification.requestPermission`, `PushManager.subscribe` with the backend's VAPID key, and idempotent server registration/unregistration. Silent on unsupported.
    - Service Worker (`public/sw.js`) gained `push` + `notificationclick` handlers. Payload shape: `{title, body, destination, id, category}`. Click routes via `postMessage` to any open client (Dashboard resolves it to `/play?frequency=...` in-app OR opens a new window if none).
    - `Dashboard.jsx` integration: bell mounted in header between Wellness Assistant sparkle and Account menu; Account dropdown gained `account-menu-notifications` item; preferences modal mounted at page bottom; new module-level `handleNotificationNavigate` maps `/play?frequency=<hz>` to `audioEngine.setFrequency + start` and `/account` to `onOpenAccount`; SW message listener wires push-tap → in-app nav.
    - **Check-in triggers wired** (all 3): (a) post-session — `pendingCheckinRef`'s existing Smart-Fade completion path now also fires `POST /me/notifications/checkin-nudge {trigger:'post_session'}`; (b) inactivity — 12-min idle timer (once per session, never during playback) fires `trigger:'inactivity'`; (c) pre-session — reserved for future feature-triggered surfaces, endpoint already accepts it.
  - **Copy guardrails**: title ≤80 chars, body ≤180 chars (Pydantic-enforced both client + server); tone always warm & non-clinical ("Want to check in for a moment?", "How are you feeling?", "A quiet moment awaits"). Never mentions diagnosis, treatment, or urgency.
  - **Gates (defense in depth)**: every `_enqueue_notification` call runs through `_can_send_notification` which checks (in order): master `enabled`, per-category enabled, quiet hours (unless `bypass_quiet=True` for feature announcements which are lower-frequency), daily cap. Frontend `checkin-nudge` endpoint returns `{delivered: false}` when gated — never surfaces the gating reason to the client (no info leak on quiet hours / cap thresholds).
  - **Analytics**: `notification_events` collection captures `sent` (with `surface: inapp` or `push`), `opened`, `dismissed`, `disabled` events. Admin endpoint `GET /api/admin/notifications/analytics` aggregates counts by (event × category × surface) plus totals for `notifications` and `push_subscriptions`.
  - **Recommendation engine**: chooses from (a) top undismissed `wellness_pattern` w/ `arm_frequency` CTA — resolves to `/play?frequency=<hz>`, (b) HB confirmed-gap center-frequency (only when `harmonic_influence_enabled=true` on assistant_settings). Once-per-calendar-day cap on `user.notif_last_rec_day`. Returns `None` (silent) if there's no honest signal — never fabricates recommendations for cold-start users.
  - **Backwards compatibility**: hooks into pre-existing patterns cache, journey rows, and HB influence toggle. Existing Phase 5–9 tests (58) still pass without modification. Pre-existing `test_hb_settings.py` 4 tests updated to match Phase 9.2's `hb_tips_skipped` field addition (loose contains-assert instead of strict shape equality).
  - **Testing** — 14 new pytest cases in `/app/backend/tests/test_notifications.py` covering VAPID key, prefs defaults + partial merge (including nested categories + quiet_hours), category & master gating on checkin nudge, invalid trigger 422, daily cap, feature announcement CRUD + audience gating + tick idempotency, mark opened/dismissed, push subscribe upsert + unsubscribe (+ push_enabled auto-flip on user doc), admin-only guards (403 for non-admin), input validation (title min-length 2), and 401 auth-gate on all user endpoints. Combined suite 72/72 pass (58 pre-existing + 14 new). Testing agent (iter 58) verified frontend E2E on 15 UI flows: bell + badge, panel render with 4 seeded announcements, dismiss/mark-all-read, both entry points into preferences modal (bell footer + Account dropdown), 5 category toggles persistence, quiet hours + daily max persistence, master-off disable state, push button visibility + accessible-disabled state.
  - **Files** — `backend/server.py` (~600 lines Phase 10 block + lifespan seed additions), `backend/requirements.txt` (pywebpush + py-vapid + http-ece), `backend/.env` (VAPID_PUBLIC_KEY, VAPID_PRIVATE_PEM, VAPID_CONTACT), `backend/tests/test_notifications.py` (new, 14 cases), `backend/tests/test_hb_settings.py` (loosened assertions for hb_tips_skipped), `frontend/src/components/NotificationBell.jsx` (new), `frontend/src/components/NotificationPreferencesModal.jsx` (new), `frontend/src/lib/pushClient.js` (new), `frontend/public/sw.js` (push + notificationclick handlers, cache bumped v4→v5), `frontend/src/components/Dashboard.jsx` (Bell import + wiring, prefs modal mount, checkin-nudge post-session hook, inactivity timer, SW message listener, account-menu-notifications item).

- **Phase 11 — Resonance / Drift Score (Feb 2026)**: Each Harmonic Blueprint capture now stores a `resonance_score` (0-100) derived from |delta_db| across bands vs the user's eigenmode. Eigenmodes = 100 by definition; follow-ups scored via `_compute_resonance_score()` in `backend/server.py`. Frontend surfaces the score via `ResonanceScoreReveal.jsx` at the end of every capture. New endpoint: `GET /api/harmonic-blueprint/resonance-score/history`. Tests: `backend/tests/test_resonance_score.py`.

- **Phase 12 — HB Gap Closure Progress + Resonance Timeline (Feb 2026, iter 59)**: Longitudinal visualisation of Harmonic Blueprint data in the Account → Harmonic Blueprint section.
  - **New endpoint** `GET /api/harmonic-blueprint/gap-progress` (Pro-gated) returns per-gap severity history + trend classification + resonance-score timeline + overall improvement summary. Trend uses ±10% hysteresis (`improving` ≥ +10%, `attention` ≤ -10%, else `stable`).
  - **Frontend cards** in `HarmonicBlueprintSection.jsx`:
    - **Gap Closure Progress** — one row per confirmed gap: horizontal progress bar (gold→teal gradient) tied to `closure_pct`, trend badge + Lucide icon (`TrendingDown`/`Minus`/`TrendingUp`), "X dB at start · N% closer/wider · Y dB now" line. Hidden when the user has no confirmed_gaps.
    - **Resonance Progress Timeline** — inline SVG line chart with area gradient fill. Gold dot for eigenmode baseline, teal dots for sessions, fixed 0-100 y-axis, dashed reference lines at 25/50/75. Encouraging summary: "Your resonance alignment has improved N% since your first session." Bottom row shows current score X/100 and legend.
  - **Tests** — `backend/tests/test_gap_progress.py` (6 cases: 401 auth gate, 402 free-user gate, payload shape, trend classification field validity, chronological timeline, summary shape). All passing.
  - **Files** — `backend/server.py` (new `harmonic_blueprint_gap_progress` endpoint at L2076), `frontend/src/components/HarmonicBlueprintSection.jsx` (new `GapClosureProgressCard`, `GapProgressRow`, `ResonanceTimelineCard` components + `progress` state + parallel fetch), `backend/tests/test_gap_progress.py` (new, 6 cases).


## Backlog (P1 → P2)

- **Phase 12b — Before/After Frequency Map (Feb 2026, iter 60)**: Side-by-side visual comparison of the user's first eigenmode baseline vs latest reading, plus a gentle progress celebration every 5th capture.
  - **New endpoint** `GET /api/harmonic-blueprint/before-after` (Pro-gated) returns baseline, latest, per-band `band_deltas` (with alignment classification `aligned` / `near` / `drift` + `improved` flag), a plain-language `summary_text` beneath the visualisation, `session_count`, and a `show_celebration` boolean that flips true every 5th non-baseline session.
  - **New shared component** `BeforeAfterMap.jsx` (default `BeforeAfterMap`, named `BeforeAfterCelebration`) — two side-by-side frequency panels labelled 'Your first baseline · {date}' and 'Your latest reading · {date}'. Baseline uses neutral gold bars. Latest uses TEAL (#72C2AC) for aligned bands, GOLD (#C4A67A) for near bands, and AMBER (#D9A45C) for drift bands. Delta pill (`Δ -5.0 dB`) shown for non-aligned latest bands.
  - **Two mount points**:
    - `HarmonicBlueprintSection.jsx` — surfaces the map inside Account → Harmonic Blueprint for anytime review.
    - `HarmonicBlueprintSheet.jsx` `confirmFindings()` — after saving a profile, fetches `/before-after` and mounts `<BeforeAfterCelebration />` (dismissible overlay with a "Continue your journey" CTA) when `show_celebration===true`.
  - **Summary phrasing** matches spec: "Your {band-labels} frequencies have strengthened significantly since you began. Your {band-labels} range continues to show some drift and remains a focus area." Falls back to "holding steady" when everything is aligned.
  - **Tests** — `backend/tests/test_before_after.py` (6 cases: auth gate, free-user gate, empty state, eigenmode-only, full shape + classification, celebration flag). All passing individually.
  - **Files** — `backend/server.py` (new endpoint + `_band_alignment` + `_describe_before_after`), `frontend/src/components/BeforeAfterMap.jsx` (new), `frontend/src/components/HarmonicBlueprintSection.jsx` (import + mount), `frontend/src/components/HarmonicBlueprintSheet.jsx` (celebrationData state, post-save fetch, overlay render), `backend/tests/test_before_after.py` (new, 6 cases).

- P1: Persisted "last used config" auto-restore on login
- P1: A/B switch between equal-temperament and Verdi-A=432 reference
- P2: Optional sleep timer (fade-out over N min)
- P2: Share a session config via short URL
- P2: Real recorded ambient tracks (rain/ocean/forest) for premium quality (would need licensed assets)
- P2: PWA install + offline mode + service-worker-driven reminders (for when tab is closed)

## Next tasks (suggested)
1. Hook "last used config" into localStorage (no backend change needed)
2. Add A/B switch for 432Hz reference
3. Daily streak counter (new MongoDB collection)
